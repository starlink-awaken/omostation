# BOS 迁移候选地图 — BET-c87a 收尾证据
# Generated: 2026-07-31
# Source: etc/bos-services.yaml (172 services)

summary:
  total: 172
  active: 167
  unimplemented: 4
  deprecated: 1

transport_distribution:
  stdio: 85        # MCP stdio 子进程 (最大类)
  mcp_stdio: 35    # 已是 MCP stdio
  inline: 27       # 内联函数
  internal: 22     # 内部模块引用
  mcp_proxy: 2     # MCP 代理
  http: 1          # HTTP (KOS REST API — 唯一)

convergence_status:
  http_services: 1  # 24→5→1 已完成
  remaining_http:
    - uri: bos://memory/kos/rest-api
      http_url: "http://localhost:8766/api/v1"
      test_coverage: "11/11 (100%)"
      status: active

# 按域分布
domains:
  capability: {total: 39, active: 39}
  governance: {total: 35, active: 34}
  memory: {total: 31, active: 30}
  analysis: {total: 27, active: 24}
  system: {total: 11, active: 11}
  persona: {total: 8, active: 8}
  meta: {total: 8, active: 8}
  agora: {total: 3, active: 3}
  cockpit: {total: 2, active: 2}
  ecos: {total: 2, active: 2}
  omo: {total: 4, active: 4}
  forge: {total: 1, active: 1}
  swarm: {total: 1, active: 1}

# 不活跃服务 (不迁移)
inactive:
  - uri: bos://memory/metaos/mcp-server
    status: deprecated
    reason: metaos 整合到 omo
  - uri: bos://analysis/iris/connect
    status: unimplemented
  - uri: bos://analysis/iris/transform
    status: unimplemented
  - uri: bos://analysis/iris/validate
    status: unimplemented
  - uri: bos://governance/protocols-layer/trigger
    status: unimplemented

# mcp_proxy 候选 (stdio → mcp_proxy)
# 判据: 已有 MCP server 进程, 可改为长驻代理
mcp_proxy_candidates:
  high_priority:
    - uri: bos://memory/kos/mcp-server
      package: kos-mcp
      reason: KOS 核心 MCP, 已有独立 server
    - uri: bos://memory/forge/mcp-server
      package: forge-mcp
      reason: aetherforge 算力框架
  medium_priority:
    - uri: bos://memory/eidos/mcp-server
      package: eidos-mcp
    - uri: bos://memory/ontoderive/mcp-server
      package: ontoderive-mcp
    - uri: bos://memory/minerva/mcp-server
      package: minerva-mcp
    - uri: bos://memory/omo/mcp-server
      package: omo-mcp

# 结论
conclusion: |
  BET-c87a http-mcp-convergence 已 95%+ 完成:
  - HTTP 收敛: 24→1 (KOS REST API, 测覆 100%)
  - MCP stdio: 29/29 正常
  - 剩余: mcp_proxy 候选评估 (可选优化, 非阻塞)
  
  建议: 标记 BET-c87a 为 DONE, mcp_proxy 优化作为后续 tech debt。
