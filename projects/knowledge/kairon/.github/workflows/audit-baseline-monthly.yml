name: kairon-audit-baseline-monthly

# B-1 P0: kairon 跨仓债审计 cron
# 目标: 跑 tools/audit.sh 检查 kairon-utils AppendOnlyLog + ContentVersionTracker + eidos 迁移状态
# 触发: 每月 1 号 01:00 UTC

on:
  schedule:
    - cron: "0 1 1 * *"
  workflow_dispatch:

jobs:
  audit-baseline:
    name: audit-baseline (B-1 P0 跨仓债)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv + Python
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          python-version: "3.13"

      - name: uv sync
        run: uv sync --quiet

      - name: Run kairon audit.sh
        run: |
          bash tools/audit.sh .
        env:
          VENV_PYTHON: .venv/bin/python
