{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://digitalbrainos.dev/schemas/member.schema.json",
  "title": "Member",
  "type": "object",
  "required": [
    "id", "type", "version", "title", "status", "owner",
    "created_at", "updated_at", "visibility", "workspace_id",
    "member_role", "policy_ref", "audit_trace"
  ],
  "properties": {
    "id": {"type": "string", "pattern": "^mbr_"},
    "type": {"const": "Member"},
    "version": {"type": "integer", "minimum": 1},
    "title": {"type": "string", "minLength": 1},
    "summary": {"type": "string"},
    "status": {"type": "string", "enum": ["active", "inactive", "pending"]},
    "owner": {"type": "string", "minLength": 1},
    "created_at": {"type": "string", "format": "date-time"},
    "updated_at": {"type": "string", "format": "date-time"},
    "visibility": {"type": "string", "enum": ["team", "org"]},
    "workspace_id": {"type": "string", "pattern": "^ws_"},
    "member_role": {"type": "string", "enum": ["owner", "operator", "reviewer", "observer"]},
    "policy_ref": {"type": "string", "minLength": 1},
    "audit_trace": {"type": "array", "items": {"type": "string"}}
  },
  "additionalProperties": true
}
