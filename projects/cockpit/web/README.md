# Cockpit Web/ Directory

> **Note**: `web/app.py` is the Agora Hub web dashboard (FastAPI, port 7430).
> It is deployed here as part of the cockpit project but belongs to the Agora (I0) layer.
> Cockpit's own web layer is a work-in-progress for the unified research entry point (L3).

See `projects/agora/` for the Agora Hub source and API documentation.
