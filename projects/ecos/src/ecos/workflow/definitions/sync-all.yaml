# Workflow: 全量同步
name: sync-all
description: "全量同步——域索引+路由缓存+注册表"
trigger: "bos://_workflow/sync-all"

steps:
  - name: domain-sync
    action: domain_sync
  - name: routes-update
    action: domain_routes
