# M2: Agent

m2_type: Agent
version: 1.0.0
created: '2026-06-06'
Agent:
  m3_parent: StructuralElement.Component
  description: AI Agent——具有特定角色·能力·工具的智能体。如 orchestrator·debugger·test-engineer。回答"谁在执行"。
  icon: 🤖
  stateMachine:
    active:
      description: active
      transitions:
      - idle
      - deprecated
    idle:
      description: idle
      transitions:
      - active
      - deprecated
    deprecated:
      description: deprecated
      transitions:
      - archived
    archived:
      description: archived
      transitions: []
  requiredProperties:
    agent_name:
      type: string
      description: Agent 名称
    role:
      type: string
      description: 角色描述
  optionalProperties:
    persona:
      type: string
      description: 人格定义
    capabilities:
      type: string
      description: 能力列表
    tools:
      type: string
      description: 工具列表
    model:
      type: string
      description: 模型
  validationRules:
  - rule: agent_name != null
    level: error
    message: Agent 必须有 agent_name
  examples:
  - id: AGENT-2026-06-06-001
    name: 示例 Agent
    description: AI Agent——具有特定角色·能力·工具的智能体。如 orchestrator·debugger·test-engineer。回答"谁在执行"。
