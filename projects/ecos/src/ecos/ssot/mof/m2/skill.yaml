# M2: Skill

m2_type: Skill
version: 1.0.0
created: '2026-06-06'
Skill:
  m3_parent: BehavioralElement.Mechanism
  description: 技能——Agent 可调用的可复用能力单元。如 gbrain skill·Scheduled task·Forge plugin。回答"Agent
    能做什么"。
  icon: 🛠️
  stateMachine:
    defined:
      description: defined
      transitions:
      - active
      - deprecated
    active:
      description: active
      transitions:
      - deprecated
      - archived
    deprecated:
      description: deprecated
      transitions:
      - archived
    archived:
      description: archived
      transitions: []
  requiredProperties:
    skill:
      type: string
      description: 技能描述
    trigger:
      type: string
      description: 触发方式 (manual/mcp/scheduled/event)
  optionalProperties:
    tools:
      type: string
      description: 使用的 MCP 工具
    agent:
      type: string
      description: 所属 Agent
    input_schema:
      type: string
      description: 输入格式
    output_schema:
      type: string
      description: 输出格式
  validationRules:
  - rule: skill != null
    level: error
    message: Skill 必须有 skill
  examples:
  - id: SKILL-2026-06-06-001
    name: 示例 Skill
    description: 技能——Agent 可调用的可复用能力单元。如 gbrain skill·Scheduled task·Forge plugin。回答"Agent
      能做什么"。
