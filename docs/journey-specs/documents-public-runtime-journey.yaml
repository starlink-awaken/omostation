schema: journey-spec/v1
status: active
journey_id: documents-public-runtime-journey
description: "公共运行时物理隔离 完整流程"
owner: governance-team
created: 2026-08-30
tags: [documents, quarantine]

states:
  - name: detected
    scene: documents-public-runtime
    description: "检测到需要处理的 documents 任务"
    next: [preflight]

  - name: preflight
    scene: documents-public-runtime
    description: "预检查：审计 + 消费者收据 + 哈希计算"
    next: [executing, failed]
    checkpoint:
      require: consumer_receipt_ok
      timeout_hours: 2

  - name: executing
    scene: documents-public-runtime
    description: "执行 quarantine 操作"
    next: [verified, rollback]
    checkpoint:
      require: hash_verified
      timeout_hours: 4

  - name: verified
    scene: documents-public-runtime
    description: "验证完成，证据已记录"
    next: [value_recorded]

  - name: value_recorded
    scene: documents-public-runtime
    description: "价值已记录，反馈到进化引擎"
    next: [complete]

  - name: failed
    scene: documents-public-runtime
    description: "执行失败"
    next: [complete]

  - name: rollback
    scene: documents-public-runtime
    description: "已回滚到初始状态"
    next: [complete]

  - name: complete
    scene: documents-public-runtime
    description: "旅程完成"
    next: []
