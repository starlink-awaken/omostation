# 运行时产物 —— 不进版本库
# 注意: gitignore 不支持行尾注释, 注释必须独占一行
run/
logs/
cache/
app-models/
backups/
__pycache__/
*.pyc

# 手工备份 —— 有了 git 就该退休; 文件留在磁盘上但不入库
*.bak-*
*.bak

# models-active/ 是**故意**入库的:
# 里面全是 alias → 外置卷权重 的 symlink, 也就是 "coding/current 现在指着谁"。
# 这正是最容易被 `omlxc use` / 手滑改掉又说不清的东西, 入库后 git 能给出历史。
# 它们是这台机器专属的, 本仓也只服务这台机器, 不存在跨机移植问题。
