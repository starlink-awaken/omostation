# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
*.egg

# ── Virtual environment ──
.venv/
venv/
.venv.bak/

# ── uv ──
# uv.lock tracked (锁依赖, CI 可重现, 含 pydantic 等传递依赖)

# ── IDE ──
.idea/
.vscode/
.serena/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db

# ── Environment & Secrets ──
.env
.env.*
*.key
*.pem
credentials.json
*.cred
auth.json

# ── Testing & Coverage ──
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ── Build artifacts ──
*.so
*.dylib
*.dll

# ── Runtime state ──
.omc/
.omo/_delivery/audit-rollout/*.json
.omo/workers/runs/
json

# ── Local historical archives (keep on disk, out of VCS) ──
.omo/_archive/demo-phase29/
.omo/_archive/probes/
.omo/tasks/archive/demo-phase29/
tests/archive/test_opc_p3_thin_binding_demo.py

# ── Docker ──
.docker/
debt/
*.tmp
runtime/omo/
omo_self_evolve.py
