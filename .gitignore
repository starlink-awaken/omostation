# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
*.egg

# ── Virtual environment ──
.venv/
venv/
.venv.bak/

# ── uv ──
uv.lock

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db

# ── Environment & Secrets ──
.env
.env.*
*.key
*.pem
credentials.json
*.cred
auth.json

# ── Testing & Coverage ──
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ── Build artifacts ──
*.so
*.dylib
*.dll

# ── Docker ──
.docker/

# MOF 快照（自动生成）
src/ecos/ssot/mof/m0/snapshot.yaml

# 数据库文件（运行时生成）
*.db
*.sqlite
*.sqlite3

# LADS/SSB 运行时链数据
LADS/
.omc/
*.tmp

# M4 derived planes (ADR-0137) — written by bin/l0-constraints-migrate.py
# and src/ecos/ssot/mof/m0/mof_driven.py. Re-built on every checkout
# from tracked SSOT, so kept out of the tree.
.omo/_derived/
