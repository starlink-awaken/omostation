---
type: ssot
---

# .kilo — 开发协作与运行辅助目录

## 作用

`.kilo/` 存放运行协作、调度与辅助工具文件，主要用于本地治理与执行链路配合，不承载产品核心源码。

## 约定

- 该目录产物应尽量可重建，避免长期保留一次性调试文件。
- 变更前先确认是否有上游持久化入口（`bin/`、`projects/*`、`.agents/`、`docs/`）。
- 目录内新增文件尽量附带来源、更新时间与用途说明，便于治理回溯。

## 快速操作

- `git status --short .kilo/`
- `ls -la .kilo/`

