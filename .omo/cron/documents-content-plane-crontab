# Documents Content Plane canonical schedule candidate
# STATUS: NOT INSTALLED — human gate required before host crontab mutation.
# This file is a two-line overlay candidate, not a replacement crontab.
# Unrelated and not-yet-migrated schedules remain unchanged on the host.
# Source evidence: docs/reports/2026-08-27-documents-consumer-audit.md
# Owner commands are Workspace-owned; Documents is a read-only input root.
# Rollback: restore the exact pre-install `crontab -l` snapshot.

# Replaces: 25 6 * * * Documents/@公共/_runtime/async-audit.py
25 6 * * * cd "$HOME/.local/share/omostation/accepted-20260908" && uv run --with pyyaml python bin/gac/documents-domain-owner-job.py consumer-audit --json --documents-root "$HOME/Documents" --workspace-root "$HOME/.local/share/omostation/accepted-20260908" --evidence .omo/_delivery/documents-plane/consumer-audit.json >> runtime/cron/documents-plane.log 2>&1

# Replaces: 35 6 * * 1 Documents/@公共/_runtime/control-plane-freshness-audit.py --report
35 6 * * 1 cd "$HOME/.local/share/omostation/accepted-20260908" && uv run --with pyyaml python bin/gac/documents-domain-owner-job.py freshness-audit --json --documents-root "$HOME/Documents" --workspace-root "$HOME/.local/share/omostation/accepted-20260908" --evidence .omo/_delivery/documents-plane/freshness-audit.json >> runtime/cron/documents-plane.log 2>&1

# Not included in this candidate: OCR, domain-index, bridge-refresh,
# convergence, signals rotation, session-brief, controller, predictor,
# watch-dispatch, daily-health, and KOS. Their existing host schedules remain
# unchanged until their own parity and rollback BETs are complete.
