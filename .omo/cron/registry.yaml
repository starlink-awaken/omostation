# 调度编译器登记源 — 单一事实源 (ADR-A, ADR-0119/0121)
# 编译产物: crontab / launchd / GitHub Actions schedule
#
# v2 (2026-09-11, TASK-B3229A65): 收敛 3 个互相矛盾的历史声明源——
#   v1 registry.yaml (10条, 全部从未真正安装) /
#   .omo/cron/operating-rhythm-crontab (14条, ADR-0119/0121, OMO_WORKSPACE_ROOT 留空从未安装) /
#   真实 `crontab -l` (10条, 唯一真的在保护系统, 但两个声明文件都不认识它)
# 每条都标 `reality`, 冲突处保留 reconciliation_note, 不静默替你做决定。
# operating-rhythm-crontab 内容已并入本文件, 原文件归档到 bin/_archive/。
version: 2
jobs:

  # ── 真实在跑 (reality: installed) ── 与当前 crontab -l 完全一致 ──

  - name: log-rotate-daily
    schedule: "0 3 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/log-rotate.py --daily >> runtime/cron/log-rotate.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      2026-09-11 修复: 原命令 bash bin/gac/log-rotate.sh 已不存在 (A5 dead-ref),
      改指向已验证可用的 bin/ssot/log-rotate.py --daily (与 operating-rhythm-crontab
      08:35 声明的脚本一致，收敛为一条，删除重复声明)。


  - name: branch-prune-daily
    schedule: "30 4 * * *"
    command: 'cd "$HOME/Workspace" && make worktree-prune >> runtime/cron/branch-prune.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      2026-09-11 修复: Makefile 的 worktree-prune 目标原调用不存在的
      bin/gac/gac-worktree-prune.sh (A4 新发现的死引用)，已改指向真实存在的
      bin/gac/gac-branch-prune.sh 并用 --dry-run 验证可用。v1 registry 原声明为
      每周一 03:30 (weekly)，真实安装是每天 04:30 (daily)；频率不同，保留真实
      运行的每日频率为准，如需改回每周请 owner 定夺 (D2)。


  - name: weekly-hygiene-patrol
    schedule: "0 7 * * 1"
    command: 'cd "$HOME/Workspace" && make hygiene-patrol >> runtime/cron/weekly-hygiene.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 纯登记债务补齐，无冲突。



  - name: ecos-kems-freshness-daily
    schedule: "30 4 * * *"
    command: >-
      cd $HOME/Workspace/projects/ecos && python3
      src/ecos/ssot/tools/mof-kems-bridge.py --freshness >>
      $HOME/Workspace/runtime/cron/kems-freshness.log 2>&1
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 纯登记债务补齐，无冲突。

  - name: mof-sla-m0-snapshot-daily
    schedule: "35 4 * * *"
    command: >-
      cd $HOME/Workspace/projects/ecos && python3 src/ecos/ssot/tools/mof-sla.py
      --snapshot-only >> $HOME/Workspace/runtime/cron/mof-sla-m0.log 2>&1
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
  - name: cli-availability-probe-6h
    schedule: "15 */6 * * *"
    command: 'cd "$HOME/Workspace" && bash bin/cli-availability-probe.sh >> runtime/cron/cli-availability-probe.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: CLI 可用性探针 (每 6 小时 00/06/12/18:15, BET-Y1Q4-T13)；满足 T13 的 6h 回归发现窗口。

  - name: ops-signal-drift-daily
    schedule: "20 6 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ops/cli.py check-signals >> runtime/cron/ops-signal-drift.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: ops check-signals 每日巡检 (每日 06:20, BET-Y1Q4-T16)，已在 crontab -l 运行，补齐登记。

  # l4-harness-6h — D7 已决议 (2026-09-11, 方案 A: dao_layer 重归类 dao→fa,
  # ecos#74): 本体矛盾消除后正式登记, CR-DFSQ-01 不再触发 (fa 允许上 cron 账本)。
  - name: l4-harness-6h
    schedule: "0 */6 * * *"
    command: >-
      cd "$HOME/Workspace" && bash projects/l4-kernel/scripts/harness-cron.sh
      >> runtime/cron/l4-harness-cron.log 2>&1
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: K
    reconciliation_note: >-
      D7 决议: l4-kernel 的 dao_layer 由 dao 重归类为 fa (道=不可执行, 而
      harness 巡检是可执行的门禁判定活动, 属法)。此前因 CR-DFSQ-01 无条件
      硬 FAIL 而"故意不登记"形成隐形债务, 重归类后登记消除声明漂移。
      脚本本体已入库 (l4-kernel#14)。

  # ── 面板刷新: 2026-09-11 退出 Codex LLM automation (token 耗尽即停摆),
  #    改用 launchd，零推理需求、零 token 依赖 ──

  - name: zhixing-dashboard-refresh
    schedule: "*/5 * * * *"
    command: >-
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/refresh.py
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >
      已验证: ~/Library/LaunchAgents/com.omostation.zhixing-dashboard-refresh.plist,
      StartInterval=300, WorkingDirectory=zhixing-dashboard 目录 (缺这个会静默不执行,
      已踩过一次坑修好)。原 Codex automation `~/.codex/automations/automation` 保持
      PAUSED, 建议后续正式归档删除, 避免和这条重复。

  # ── knowledge-foundry-6h: P76 Phase6 治理编排 (14 子任务) ──
  # plist 已随仓安装且与 repo 一致, 但从未 launchctl load (无 foundry.log 历史),
  # spec (docs/architecture/knowledge-foundry-cron.md) 仍为 draft。
  # 不激活原因: brief-gen 子任务每 6h 写 BRIEF.md (tracked) 到主树,
  # 与 ADR-4443 v8/P96 主树只读纪律冲突; 激活前需先解决输出落点
  # (worktree 提交流 或 BRIEF.md 迁 runtime/)。登记 = 把隐形债务转为
  # 可观测的待决策项, 不代表激活。status=proposed 故 scheduler-compile
  # 不校验 drift。

  - name: knowledge-foundry-6h
    schedule: "0 0,6,12,18 * * *"
    command: >-
      cd $WORKSPACE && uv run python bin/gac/knowledge-foundry-cron.py
      >> $HOME/.local/state/omostation/foundry.log 2>&1
    planes: [launchd]
    reality: installed
    status: proposed
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      plist ~/Library/LaunchAgents/com.omostation.knowledge-foundry.plist
      与 runtime/cron/ 版本一致 (2026-09-12 校验), 但 launchctl 未 load,
      无运行日志。14 子任务 (omo-sync/compliance/p74/mof-drift/m4-health/
      debt/bootloader/submodule-bump/gitlink/swarm-window/brief-gen/port-governance/
      memory-os-consolidate) 全 dormant。激活阻塞: brief-gen 写 tracked BRIEF.md
      违反主树只读 (ADR-4443 v8); 需先改输出落点。meta-doctor 2026-09-12 发现。

  # ── 孤儿 crontab 补登 (2026-09-12): 实际运行但 registry 未登记 ──
  - name: worktree-hygiene-daily
    schedule: "0 4 * * *"
    command: 'cd "$HOME/Workspace" && make worktree-hygiene >> runtime/cron/worktree-hygiene.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: worktree 卫生日检 (每日 04:00)，已在 crontab -l 运行，补齐登记。

  # ── 债务复审日调度补登 (2026-09-20, #4087 交付物) ──
  - name: debt-review-daily
    schedule: "13 7 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/gac/debt-review-scheduler.py >> runtime/cron/debt-review.log 2>&1 # omostation-debt-review (债务定期复审调度, 2026-09-20)'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >-
      PR #4087 交付的债务定期复审调度器 (终态无实证 + 逾期未复审报告)。
      2026-09-20 安装 crontab 时漏登记, 造成 A4 Scheduler 门禁 orphan;
      本条为登记↔安装同改的补账。

  - name: worktree-retire-14d-weekly
    schedule: "30 7 * * 1"
    command: 'cd "$HOME/Workspace" && make worktree-hygiene-retire >> runtime/cron/worktree-hygiene.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      GOVERNANCE.md stale worktree retirement (N=14 天闲置, #3864 docs-only)
      的自动化落点：--stale-days 14 + dry-run 只报告不删除（无 --execute，
      归档 diff 后再删仍走人工标准释放流程），语义与 worktree-hygiene-daily
      一致。declared_only + proposed，按 D4 惯例不进 scheduler-compile
      drift 校验；正式启用（安装 crontab）需单独决定，启用时把本条翻为
      installed/active 并同步安装，登记与安装同改以杜绝 TASK-B3229A65
      式 double-write 漂移。

  - name: worktree-prune-daily
    schedule: "30 4 * * *"
    command: 'cd "$HOME/Workspace" && make worktree-prune >> runtime/cron/branch-prune.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: worktree 修剪 (每日 04:30)，已在 crontab -l 运行，补齐登记。

  - name: hygiene-dir-daily
    schedule: "0 5 * * *"
    command: 'cd "$HOME/Workspace" && make hygiene-dir >> runtime/cron/hygiene-dir.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 目录卫生日检 (每日 05:00)，已在 crontab -l 运行，补齐登记。

  - name: hygiene-patrol-weekly
    schedule: "0 7 * * 1"
    command: 'cd "$HOME/Workspace" && make hygiene-patrol >> runtime/cron/weekly-hygiene.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 周巡检 (每周一 07:00)，已在 crontab -l 运行，补齐登记。

  - name: ssot-guardian-weekly
    schedule: "0 6 * * 3"
    command: 'cd "$HOME/Workspace" && make ssot-guardian >> runtime/cron/ssot-sync.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: SSOT 守护 (每周三 06:00)，已在 crontab -l 运行，补齐登记。

  - name: capability-registry-sync-daily
    schedule: "5 4 * * *"
    command: 'cd "$HOME/Workspace" && git add docs/generated/capability-registry.yaml docs/INDEX-MCP.md docs/CLI-REFERENCE.md 2>/dev/null && git diff --cached --quiet || git commit -m "chore(auto): capability-registry daily sync [skip ci]" >> runtime/cron/capability-registry-sync.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: capability-registry 每日同步 (04:05)，已在 crontab -l 运行，补齐登记。

  - name: kems-freshness-daily
    schedule: "30 4 * * *"
    command: 'cd /Users/xiamingxing/Workspace/projects/ecos && python3 src/ecos/ssot/tools/mof-kems-bridge.py --freshness >> /Users/xiamingxing/Workspace/runtime/cron/kems-freshness.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: KEMS 保鲜度日检 (04:30, ecos 子模块)，已在 crontab -l 运行，补齐登记。

  - name: mof-sla-snapshot-daily
    schedule: "35 4 * * *"
    command: 'cd /Users/xiamingxing/Workspace/projects/ecos && python3 src/ecos/ssot/tools/mof-sla.py --snapshot-only >> /Users/xiamingxing/Workspace/runtime/cron/mof-sla-m0.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: MOF SLA M0 快照 (04:35, ecos 子模块)，已在 crontab -l 运行，补齐登记。

  - name: l4-harness-6h-active
    schedule: "0 */6 * * *"
    command: 'cd /Users/xiamingxing/Workspace && bash projects/l4-kernel/scripts/harness-cron.sh >> runtime/cron/l4-harness-cron.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: K
    reconciliation_note: l4-kernel harness 巡检 (每 6h)，已在 crontab -l 运行。D7 决议后正式登记为 active (sfop_slot K, 法)。

  - name: scene-calibration-weekly-active
    schedule: "0 6 * * 1"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/ssot/calibration-engine.py daily >> runtime/cron/scene-calibration.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 场景校准引擎 (每周一 06:00)，已在 crontab -l 运行，补齐登记。

  - name: scene-registry-sync-daily-active
    schedule: "15 6 * * *"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/ssot/scene-v3-registry.py generate >> runtime/cron/scene-registry.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: 场景注册同步 (每日 06:15)，已在 crontab -l 运行，补齐登记。

  - name: zhixing-host-drift-hourly-active
    schedule: "23 * * * *"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/gac/workspace-wip-guard.py protect && python3 bin/gac/zhixing-host-sync.py check'
    planes: [launchd]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: "织星宿主漂移 + 未推送提交钉扎 每小时巡检 (2026-09-19)。**crontab 写入在系统层已坏** (连原样重写都返回 Interrupted system call; /var/at/tabs 为 root:wheel drwx------), 故改用 launchd 承载 —— plist 源 bin/ops/launchd/com.omostation.zhixing-host-drift.plist, 日志 runtime/cron/zhixing-host-drift.log。"

  - name: zhixing-panel-sync-hourly-active
    schedule: "23 * * * *"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/gac/zhixing-panel-sync.py ensure --quiet >> runtime/cron/zhixing-panel-sync.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: "织星驾驶舱场景面板幂等自愈 (每小时第 23 分)；ensure 现内嵌 host-file 漂移检查，避免依赖 setuid crontab 重装。部署目录不受版本控制, 面板曾被他 agent 覆盖 2 次; 资产版本化于 bin/panorama/assets/scene-panel.html。"

  - name: wip-guard-hourly-active
    schedule: "17 * * * *"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/gac/workspace-wip-guard.py snapshot --reason cron-hourly --quiet && python3 bin/gac/workspace-wip-guard.py protect >> runtime/cron/wip-snapshot.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: "主工作区 WIP 快照 (每小时第 17 分) — 共享工作区被并发会话切换/覆盖时兜底; 2026-09-17 实证改动丢失事故。"

  - name: scene-history-daily-active
    schedule: "20 3 * * *"
    command: 'cd /Users/xiamingxing/Workspace && python3 bin/ssot/scene-history.py export && python3 bin/ssot/scene-history.py backup && python3 bin/ssot/scene-history.py verify >> runtime/cron/scene-history.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: "场景历史耐久性 (每日 03:20) — 持久导出 + 本地快照(保留14) + 行数暴跌检测; verify 失败退出非零, 防 2026-09-17 校准数据静默清空复发。"

  # ── 面板刷新: 织星全景驾驶舱（BET-Y1Q4-T10-163, 2026-09-12 登记）──
  # 常驻静态服务(KeepAlive) + 服务内 5min 自动刷新; 与 Serena 观测站(43191)互补。

  - name: panorama-dashboard
    schedule: "always"
    command: >-
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/panorama-serve.py --port 43910 --no-refresh
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >
      plist ~/Library/LaunchAgents/com.omostation.panorama-dashboard.plist
      (runtime/cron/ 同源)。常驻 serve(KeepAlive=true) 只负责静态站点；
      刷新由 panorama-dashboard-refresh 独立执行，避免共享主仓旧 collector
      覆盖最新运行态投影。产物 runtime/dashboard/(gitignored)。

  - name: panorama-dashboard-refresh
    schedule: "*/4 * * * *"
    command: >-
      PANORAMA_CODE_ROOT=/Users/xiamingxing/.local/share/zhixing-dashboard/code-main
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/panorama-collect.py
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >-
      部署版 collector 显式通过 PANORAMA_ROOT 指向 canonical Workspace，
      避免共享主仓本地 checkout 落后时覆盖 Agent Cell 等 origin/main 投影。
      PANORAMA_CODE_ROOT 指向受管 fresh-main 引用根，分离运行态真值与
      gate 引用/registry 代码真值，避免旧 checkout 把 A2/A4/A5 投影为假失败。
      4分钟频率与 Agent Cell smoke 闭环。plist 同源:
      runtime/cron/com.omostation.panorama-dashboard-refresh.plist。

  - name: agent-cell-pool-live-smoke
    schedule: "*/4 * * * *"
    command: >-
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/agent-cell-pool-live-smoke.py
      --state-file /Users/xiamingxing/Workspace/.omo/state/agent-cell/cell_states.json
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >-
      受控 AGE-v2 CellPool 生命周期探针: planner→executor→verifier→idle，
      仅写 .omo/state/agent-cell/ durable state 与 hash-chain receipt，
      无外部副作用。4分钟频率保持 Panorama 5分钟新鲜窗口可见。
      运行脚本部署在 zhixing-dashboard runtime 目录（与刷新器一致），
      显式写回 canonical OMO runtime state。plist 同源:
      runtime/cron/com.omostation.agent-cell-pool-live-smoke.plist。

  - name: agent-cell-semantic-smoke
    schedule: "17 */6 * * *"
    command: >-
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/agent-cell-semantic-smoke.py
      --state-dir /Users/xiamingxing/Workspace/.omo/state/agent-cell/semantic
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >-
      持久 RoleRegistry、CapsuleStore、Workflow Mesh handoff 与 Resident
      TaskQueue 闭环 canary；只写 canonical semantic runtime state，
      不调用 Claims Authority、不产生外部副作用、不作价值证明。
      plist 同源: runtime/cron/com.omostation.agent-cell-semantic-smoke.plist。

  - name: reference-cell-direct-local-smoke
    schedule: "0 */12 * * *"
    command: >-
      PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 -B
      /Users/xiamingxing/.local/share/zhixing-dashboard/reference-cell-direct-local-smoke.py
      --code-root /Users/xiamingxing/.local/share/zhixing-dashboard/code-main
      --attempts-root /Users/xiamingxing/agents/codex-agent-os-reference-cell/attempts
      --omo-src /Users/xiamingxing/Workspace/projects/omo/src --json
    planes: [launchd]
    reality: installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: >-
      attempt-local Direct Local Reference Cell R0 canary；只读读取
      ARCHITECTURE.md，只写 attempt 外部证据目录，不写 Workspace、不调用
      Claims Authority、不作价值证明。12小时频率保持 RC-DL 在 168小时
      新鲜窗口内。plist 同源:
      runtime/cron/com.omostation.reference-cell-direct-local-smoke.plist。

  # ── 从未真正安装, 只存在于声明文件里 (reality: declared_only) ──
  # 以下 10 条来自 operating-rhythm-crontab (ADR-0119/0121), 设计有 fail-closed
  # 防护 (OMO_WORKSPACE_ROOT 校验), 但从未真正 `crontab` 安装过。是否现在正式启用
  # 是新增真实自动化的决定 (D4), 本次收敛只做文档统一, 不擅自激活。
  # 若启用: 需要真实设置 OMO_WORKSPACE_ROOT=/Users/xiamingxing/Workspace (原模板留空)。

  - name: agent-workflow-status-daily
    schedule: "0 9 * * *"
    command: >-
      cd "$HOME/Workspace" && bash bin/gac/heartbeat-wrapper.sh
      agent-workflow-status uv run --with pyyaml python bin/agent-workflow.py
      status --json >> runtime/cron/operating-rhythm-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: governance-evolution-status-daily
    schedule: "5 9 * * *"
    command: >-
      cd "$HOME/Workspace" && uv run --with pyyaml python
      bin/gac/governance-evolution.py status --json >>
      runtime/cron/operating-rhythm-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: governance-evolution-packages-daily
    schedule: "10 9 * * *"
    command: >-
      cd "$HOME/Workspace" && uv run --with pyyaml python
      bin/gac/governance-evolution.py packages --json >>
      runtime/cron/operating-rhythm-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: m4-health-score-daily
    schedule: "15 9 * * *"
    command: >-
      cd "$HOME/Workspace" && uv run --with pyyaml python
      bin/mof/m4-health-score.py --emit >> runtime/cron/operating-rhythm-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: agent-workflow-doctor-daily
    schedule: "20 9 * * *"
    command: >-
      cd "$HOME/Workspace" && uv run --with pyyaml python
      bin/agent-workflow.py doctor --json >> runtime/cron/operating-rhythm-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: mof-drift-weekly
    schedule: "5 10 * * 1"
    command: 'cd "$HOME/Workspace" && bin/mof/mof-drift >> runtime/cron/operating-rhythm-weekly.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: debt-refresh-weekly
    schedule: "30 10 * * 1"
    command: >-
      cd "$HOME/Workspace" && uv run --project projects/omo omo debt refresh
      --now "$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> runtime/cron/operating-rhythm-weekly.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: weekly-review-post-inbox
    schedule: "35 10 * * 1"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/weekly-review.py --post-inbox >> runtime/cron/weekly-review.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: drift-sweep-weekly
    schedule: "0 9 * * 3"
    command: >-
      cd "$HOME/Workspace" && bash bin/gac/heartbeat-wrapper.sh drift-sweep
      python3 bin/gac/drift-sweep.py --json >> runtime/cron/drift-sweep.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: capability-ownership-daily
    schedule: "30 8 * * *"
    command: >-
      cd "$HOME/Workspace" && bash bin/gac/heartbeat-wrapper.sh
      capability-ownership python3 bin/gac/check-capability-ownership.py --json
      >> runtime/cron/capability-ownership.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: observability-events-adapters-daily
    schedule: "30 8 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/observability-events.py adapters run >> runtime/cron/observability-daily.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: anomaly-alert-daily
    schedule: "45 8 * * *"
    command: >-
      cd "$HOME/Workspace" && python3 bin/gac/anomaly-detector.py --json >>
      runtime/cron/observability-daily.log 2>&1 && python3 bin/gac/alert-router.py
      --window 20 >> runtime/cron/observability-daily.log 2>&1
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: meta-doctor-daily
    schedule: "25 9 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/gac/meta-doctor.py --json >> runtime/cron/operating-rhythm-daily.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S
    reconciliation_note: v1 registry 与 operating-rhythm-crontab 各有一份几乎相同定义, 已收敛为这一条。

  - name: unified-health-score-sync-daily
    schedule: "30 9 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/gac/unified-health-score.py --sync >> runtime/cron/operating-rhythm-daily.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: rules-lifecycle-weekly
    schedule: "0 9 * * 1"
    command: 'cd "$HOME/Workspace" && python3 bin/gac/rules-lifecycle.py >> .omo/_log/rules-lifecycle.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S
    reconciliation_note: ADR-0431 D2 要求的规则生命周期扫描, 目前完全没有在跑。

  - name: scene-calibration-weekly
    schedule: "0 6 * * 1"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/calibration-engine.py daily >> runtime/cron/scene-calibration.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: scene-registry-sync-daily
    schedule: "15 6 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/scene-v3-registry.py generate >> runtime/cron/scene-registry.log 2>&1'
    planes: [crontab]
    reality: declared_only
    status: proposed
    owner: governance-team
    sfop_slot: S

  - name: scene-guardrail-daily
    schedule: "30 6 * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/scene-v3-guardrail.py validate-all --allow-forward-refs >> runtime/cron/scene-guardrail.log 2>&1'
    planes: [crontab]
    reality: crontab_installed
    status: active
    owner: governance-team
    sfop_slot: S

  - name: scene-signal-poll
    schedule: "*/15 9-18 * * 1-5"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/scene-signal-poller.py poll >> runtime/cron/scene-signal-poll.log 2>&1'
    planes: [crontab]
    reality: crontab_installed
    status: active
    owner: governance-team
    sfop_slot: S
    note: "工作日 9-18 点每 15 分钟轮询邮件信号并触发场景 (assisted+ live, shadow dry-run)"

  - name: remote-hygiene-hourly
    schedule: "23 * * * *"
    command: 'cd "$HOME/Workspace" && python3 bin/ssot/fix-remotes.py >> runtime/cron/remote-hygiene.log 2>&1 # omostation-remote-hygiene (origin 污染幂等自愈, 2026-09-13 实证事故)'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      2026-09-14 补登 PR #3752/#3754 遗漏：该 hourly remote-hygiene job 已安装且
      使用 fix-remotes.py，但未进入 scheduler registry，导致 A4 orphan=1。
      本条只补声明面 parity，不改变已安装 schedule、command 或运行行为。

  - name: zhixing-dashboard-panorama-cache-5min
    schedule: "*/5 * * * *"
    command: 'cd /Users/xiamingxing/.local/share/zhixing-dashboard && python3 panorama_cache.py >> /tmp/panorama_cron.log 2>&1'
    planes: [crontab]
    reality: installed
    status: active
    owner: governance-team
    sfop_slot: S
    reconciliation_note: >
      2026-09-16 补登 (TASK-B3229A65 收尾)：scheduler-compile.py --check 报告的最后
      1 个 orphan。与上面已登记的 zhixing-dashboard-refresh（同一面板、已于
      2026-09-11 迁 launchd 脱离 Codex 依赖）是两条独立并存的任务，不是同一个
      被替代掉的脚本——panorama_cache.py 走的是普通 crontab、纯本地缓存刷新，
      本来就不含 LLM/Codex 调用，此前只是漏登记。本条只补声明面 parity，不改变
      已安装 schedule、command 或运行行为。

  - name: mimo-models-sync
    schedule: '*/30 * * * *'
    command: PATH=/Users/xiamingxing/.mimocode/bin:/usr/bin:/bin /Users/xiamingxing/.local/bin/mimo-models-sync.sh > /tmp/mimo-sync.log 2>&1
    planes:
    - crontab
    reality: crontab_installed
    status: active
    owner: agent-experience-steward
    sfop_slot: S
    reconciliation_note: '2026-09-22 登记: 此前 crontab 中以 # 待登记外部工具 尾注游离存在,
      导致 A4 orphan_count=1。脚本为 ~/.local/bin 外部工具, 每 30min 同步 Ollama/LMStudio/OMLXC
      本地模型到 mimo 配置; 登记进本账本使 scheduler-compile --check 归零。
      '
# ── 待人工一次性拍板 (不影响本次收敛的文档统一, 只影响是否改变真实运行行为) ──
open_decisions:
  - id: D2
    question: worktree-hygiene 是否要把 --stale-days 3 补回 make 目标? branch-prune 频率日/周哪个对?
  - id: D3
    question: ssot-guardian 频率到底是每日还是每周三? 当前保留真实运行的每周三。
  - id: D4
    question: >-
      上面 17 条 status: proposed 的治理巡检任务(agent-workflow-status/governance-evolution/
      m4-health-score/doctor/mof-drift/debt-refresh/weekly-review/drift-sweep/
      capability-ownership/observability/anomaly-alert/meta-doctor/unified-health-score/
      rules-lifecycle/scene-calibration/scene-registry-sync) 是否现在正式启用为真实
      crontab? 这是新增自动化, 不是文档整理, 需要单独决定, 启用前需设置真实
      OMO_WORKSPACE_ROOT。
  - id: D7
    status: resolved
    resolved_at: '2026-09-11'
    resolution: >-
      方案 A (重归类): l4-kernel dao_layer dao→fa (ecos#74), l4-harness-6h
      正式登记进本 registry。理由: 道=不可执行, harness 巡检是可执行门禁
      判定活动属法; 全仓唯一 dao 归类系命名直觉。归类指引见
      docs/architecture/dao-fa-shu-qi.md。
    question: >-
      真实在跑的 l4-harness-6h(每6小时跑一次 projects/l4-kernel 的 harness 巡检)
      违反 CR-DFSQ-01(dao 层组件不得出现在 cron 执行账本), 且已经违反了一段时间
      (不是本次改动引入)。故意没有登记进本文件(登记会让 check-sfop-slots.py 硬性
      FAIL)。需要决定: 停用这条 cron、把 harness 巡检挪到 cron 之外的机制、还是
      重新评估 l4-kernel 的 dao_layer 归类。

# 外部合法孤儿: 不属于本工作区调度 SSOT、经确认保留在 crontab 的条目
# (scheduler-compile.py check_drift 用子串匹配豁免)
known_orphans:
  - clash-probe.sh  # 用户本机网络探测 (~/.local/bin), 2026-09-20 确认为外部工具
  - mimo-models-sync.sh  # 用户本机外部模型同步 (~/.local/bin), 2026-09-22 确认为外部工具
  - clash-health.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-proxy-guard.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-traffic.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-cert-check.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-bw-warn.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-git-sync.sh  # 用户本机 Clash 代理维护 (~/.local/bin→~/.config/clash), 2026-09-22 确认为外部工具
  - clash-node-watch.sh  # 用户本机 Clash 节点守护 (~/.local/bin), 2026-09-22 确认为外部工具
  - fleet-watch.sh  # 用户本机 Clash fleet 巡检 (~/.local/bin→~/.config/clash), 2026-09-23 确认为外部工具
  - refresh-cf-ips.sh  # 用户本机 Clash CF 优选 IP 巡检 report-only (~/.local/bin→~/.config/clash), 2026-09-23 确认为外部工具
  - bwg-usage.sh  # 用户本机带宽用量 (~/.local/bin), 2026-09-23 确认为外部工具
  - conf-backup.sh  # 用户本机配置备份 (~/.local/bin), 2026-09-23 确认为外部工具
