# Phase 3C: 场景模板 + Pipeline 完全体 (已完成)

## 任务清单

### T1: 预置管线模板
- `eidos/src/eidos/pipeline/presets.py` — knowledge-base / reasoning 预设

### T2: Pipeline 输出链完成
- kos search handler — `--pipeline-output` 实际写入文件
- minerva research handler — `--pipeline-output` 实际写入文件
- ontoderive derive handler — `--pipeline-output` 实际写入文件

### T3: Pipeline 端到端可用
- `eidos pipeline --name knowledge-base` 可运行
- `eidos pipeline --name reasoning` 可运行

### T4: 全量测试通过
- 50 + 7 + new = 所有测试通过
