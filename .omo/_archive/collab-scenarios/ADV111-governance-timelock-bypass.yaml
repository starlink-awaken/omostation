---
status: active
lifecycle: ssot
owner: red-team
last-reviewed: 2026-07-29
---
id: ADV111-governance-timelock-bypass
category: A_conflict
adversarial: true
seed: 211
description: >
  [对抗·red-team wave20] 治理时锁绕过: 紧急角色跳过 delay 直接执行提案,
  管线已有 governance_timelock_bypass_detected → 预期 PASS
setup:
  blackboard:
    - {key: timelock_queue, value: "pending_48h", writers: [timelock]}
  roles: [proposer, guardian, executor]
inject:
  - {type: write_conflict, role: proposer, target: proposal, value: "set_param_critical"}
  - {type: write_conflict, role: guardian, target: emergency_role, value: "grant_self_executor"}
  - {type: write_conflict, role: executor, target: timelock_queue, value: "skip_delay_execute_now"}
  - {type: write_conflict, role: executor, target: system_param, value: "malicious_value"}
expected:
  behavior: governance_timelock_bypass_detected
  silent_loss: 0
verdict:
  - criterion: governance_timelock_bypass_detected
    check: events_contain
    args: {kind: governance_timelock_bypass_detected}
  - criterion: no_silent_loss
    check: silent_loss_eq
    args: {expected: 0}
