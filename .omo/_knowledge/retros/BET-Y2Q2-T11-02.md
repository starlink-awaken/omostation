---
schema: md/v1
status: active
lifecycle: entry
owner: auto-fix-loop
last-reviewed: 2026-09-25
type: ephemeral
---

# BET-Y2Q2-T11-02 Retro

- Q1 实际耗时 vs appetite：约 1 天内完成；未超出 1 day appetite。根因定位额外发现两次旧 workflow 绑定漂移，已关闭为 blocked 并重新绑定完整 surface。
- Q2 done_when：全部达成。`gac-worktree.sh` 在 bulk submodule init 前执行递归 `git submodule sync`；回归测试证明陈旧本地 URL 不再控制新 worktree；`SKIP_SUBMODULE_INIT=1` 原测试继续通过；shell、focused pytest、GAC local gate、SSOT guardian 和远端 CI 均通过。
- Q3 与 plan 不符的事实：初始失败不是远端 gitlink 不可达。`origin/main` 的四个关键 gitlink 均与远端子仓 `main` 一致；失败来自根仓本地 `.git/config` 的 `submodule.<path>.url` 覆盖被新 worktree 继承。另发现完整 ledger BET 必须包含 `window`、`workflow` 和 retro write surface，已补齐。
- Q4 净增减：根脚本新增 6 行；新增 1 个行为回归测试（测试文件新增 23 行）；新增 spec、plan、bootstrap waiver、retro 与 BET 登记。未移动 gitlink，未修改 `.gitmodules`，未改子仓源码。
- Q5 后续 agent 须知：创建 root worktree 后不能直接初始化子模块；必须先同步 `.gitmodules` 到 worktree config。若初始化仍失败，再检查远端 ref 可达性和根仓 tracking ref，不要 force checkout 或手工改 gitlink。

## Delivery Evidence

- root PR #4276 merged as `05e80b88094eb8301830665a65c3a0d21090b16a`
- root CI: GAC gate, governance verify, integration, interface-check, test, evidence gate, and BET done gate all passed
- root tag: `bet/BET-Y2Q2-T11-INTEGRATION-20260924T063950Z`
- `uv run --with pytest pytest -q tests/test_gac_worktree_claim_pasw.py` → 13 passed
- `bash -n bin/gac/gac-worktree.sh` → pass
- `make gac-local-gate` → 57 checks passed
- `make ssot-guardian` → pass
- `git diff --check` → pass
