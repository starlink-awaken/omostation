# BET-Y1Q4-T10-03 复盘

## Q1 实际耗时 vs appetite？超出比例？

- 实际耗时：约 2 小时，主要用于文档事实核对、架构归纳、workflow 绑定和 closeout 闭环。
- appetite：本 bet 的目标以文档/分析输出为主，实际属于轻量设计与治理文档任务，未预期大规模代码改动。
- 结论：未明显超出 appetite，属于轻度偏差；主要消耗在反复核对仓库治理规则与 workflow 约束，而不是业务实现。

## Q2 done_when 是否全部通过？哪条没过，为什么？

- 主要 done_when：
  1. 形成架构分析结论：已完成。
  2. 区分主线架构与执行适配层：已完成。
  3. 输出完整需求与优化方案：已完成。
  4. 以 bet-ledger / spec / workflow 机制绑定：已完成。
- 未通过项：最终 `make agent-workflow-closeout` 在第一次执行时因为缺少 retro file 被拦住，此为仓库治理要求，而非分析结论问题。
- 原因：workflow 设计要求在置 done 前必须写 `.omo/_knowledge/retros/<BET-ID>.md`，并在北极星链路中绑定。缺失 retro 会触发 vision→retro 链断裂。

## Q3 过程中发现的与 plan 不符的事实（打假）？

- 之前认为“只要写好文档和 bet 就能直接 closeout”，但仓库实际要求严格的 retro、北极星链路和 workflow 绑定。
- 之前对 `multica` / `orca` / `pi-worker` 等组件的理解一度可能带有“工具名即主线架构”的误区，实际它们更准确地被视为执行后端或适配层，不是系统核心控制面。
- 真实的关键区别不是“谁在目录里看起来重要”，而是“谁承担统一控制面，谁仅负责执行/适配”；这个结论后来成为整篇分析的核心判断。
- 该任务的“分析”并不等于“代码落地”，它更接近治理型 / 设计型 bet，因此 workflow 和文档证据链比实现代码更关键。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？（贴 surface 输出）

- 变更文件：
  - `docs/plans/2026-09-04-architecture-analysis-and-requirements-consolidation.md`
  - `docs/plans/3y-bet-ledger.yaml`
  - `docs/superpowers/specs/2026-09-04-architecture-analysis-requirements-spec.md`
  - `.omo/_knowledge/retros/BET-Y1Q4-T10-03.md`
- 代码行变化：以文档与 ledger 结构化记录为主，属于非代码增量；净增为正，但无功能代码改动。
- 规则/ADR/脚本：未新增 GaC 规则、ADR、脚本；此次工作保持在分析 / 需求 / bet 记录层，不扩张系统表面积。
- 结论：本 bet 对项目体积是“治理分析性增量”，而非功能性扩张。

## Q5 下一个认领本 track 的 agent 需要知道什么？

- `omostation` 的核心不是单体应用，而是治理驱动的 Agent OS / 多项目工作区。
- 真正关键的控制面是 workflow + governance + resident runtime + BOS routing，而不是各类 adapter 的名称。
- 多 Agent 协作的核心机制是：worktree 隔离、claim、verify、closeout、bet 绑定与 SSOT 事实源。
- `orca` / `multica` / `pi-worker` 属于执行适配层；它们可能扩展执行能力，但不能替代主线治理控制面。
- 任何后续架构收敛工作，都应优先统一控制面和执行边界，再处理适配层扩展，以避免出现第二 dispatcher / 失控适配层。
- 落地时必须先做 workflow start -> claim -> verify -> closeout 的闭环，不能跳过 governance 证据链。

---

## 2026-09-05 追加 — HITL 第 2 次生产化采用 (bet-execution)

**背景**: HITL Proposal System v1.0 已落地(PR #3077 + #129 + #3119 + #3120 + #3135 合并),本 BET-Y1Q4-T10-03 通过 HITL 流程 closeout,作为第 2 个生产化采用者(继 BET-Y1Q4-T8-04 之后)。

**HITL E2E 流程**:
1. `bin/hitl-proposal.py check --bet-id BET-Y1Q4-T10-03` → `HITL_REQUIRED`
2. `bin/hitl-proposal.py create ...` → proposal `hitl-20260905002432-2df9c0`
3. `bin/cockpit decide list` → 显示
4. `bin/cockpit decide approve <id>` → approved
5. actor 自动捕获: `xiamingxing <234556587+starlink-awaken@users.noreply.github.com>`

**Adoption 贡献 (BET-Y1Q4-T1-12 跟踪)**:
- L2/L0 human_gate BET 启用率: 2/120 (1.7%)
- adoption runbook 引用: 2 个 (T8-04 retro + 本追加)
- 不同 BET id approved proposals: 2

**模板沉淀**: HITL closeout 流程已标准化,任何 human_gate=true 的 BET closeout 步骤:
1. 验证 `verify` 命令 exit 0
2. 走 HITL 流程 (check→create→list→approve)
3. ledger 改 status 为 done
4. 在 retro 追加 HITL adoption 段落

下一次采用者请直接复用本模板。
