---
type: ephemeral
status: archived
---

# BET-Y1Q4-T10-05 复盘（batch 1：spec 绑定 + 最小闭环）

## Q1 实际耗时 vs appetite？超出比例？
Appetite 3 days。本 batch 为单会话交付：spec 新建 + ledger 回填 + vault 最小闭环（vault.py/config/自测 5/5）。
未超 appetite；剩余 budget 留给 batch 2（Spine 拟草/署名演练集成 + ledger verify 口径对齐）。

## Q2 done_when 是否全部通过？
batch 1 覆盖 ledger done_when 中的子集：
1. Local Secure Vault 零明文存储实现 — 通过（envelope 仅 salt/nonce/ciphertext/tag，自测断言落盘无明文）。
2. 断网离线状态全套 Spine 拟草与署名演练 — 未做，batch 2。
3. 集成测试覆盖防注入与权限隔离 — 部分（防篡改 AuthError + 锁定熔断已测；权限隔离 batch 2）。
4. circuit_breaker（失败锁定）— 通过（3 次失败锁定，显式 reset 解锁）。

## Q3 过程中发现的与 plan 不符的事实（打假）？
1. **yaml.safe_dump 全量回写是格式破坏器**：首次回填 ledger 用 safe_dump 导致 2125+/3222- 全文件重排，已回滚改外科手术式文本编辑（diff 仅 +8/-1 行）。
2. **worktree 内 ecos 子模块工作树被清空**（全部 D）：`git submodule update --force --checkout` 恢复后 compiler 正常，start 通过。
3. **ledger verify 口径与 write_surfaces 不一致**：verify 写 `security.vault.test_offline_sandbox`，
   但 write_surface 是文件 `bin/security/vault.py`（vault 无子模块）；实际可跑口径为
   `PYTHONPATH=bin python -m security.test_offline_sandbox`。batch 2 需二选一：vault/ 包化或改 ledger verify。
4. **write_surface 与 .gitignore 打架**：ledger 点名 `config/security/`，但根 `.gitignore` 全局忽略 `config/`；
   本 batch 用 `git add -f` 只收录无秘密的策略文件 `vault.yaml`（不碰 gitignore）；run state 与
   affected-graph receipt 按惯例不入库（历史 runs/ 均 untracked）。
   batch 2 若 config 下文件增多，需正式 decision：gitignore 收窄 vs 迁移到版本化目录。

## Q4 净增减
- spec：+1（docs/superpowers/specs/2026-09-11-t10-05-offline-sandbox-vault-spec.md，sha256:ecbf0173…fac30）
- 台账：T10-05 +accepted_specifications（1 条）+ write_surfaces 追 spec 路径
- 代码：bin/security/vault.py（stdlib 密封/揭示/熔断）+ bin/security/test_offline_sandbox.py（5/5）
- 配置：config/security/vault.yaml（零明文/KDF/熔断/离线四策略）

## Q5 下一个 agent 需要知道什么？
1. ledger 编辑禁止 yaml 全量 dump，只能定点 edit。
2. batch 2 先定 verify 口径（vault/ 包化 vs 改 ledger），再做 Spine 演练集成。
3. run-id：20260911T062617Z-bet-execution-27ea1336；claim 含 config/security、vault.py、retro。
