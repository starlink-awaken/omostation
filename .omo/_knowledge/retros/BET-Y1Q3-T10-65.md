---
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-08-29
title: BET-Y1Q3-T10-65 Capability-sync mesh extraction retro
type: retro
---

# BET-Y1Q3-T10-65 复盘

## Q1 实际耗时 vs appetite

约 0.25 day（符合）。额外耗在目录选位教训：helper 初放 `bin/`，测试用
spec_from_file_location 加载时 `bin/` 不在 sys.path → 120 errors；兄弟模块
的家在 `lib/`（capability-sync 自身的 LIB bootstrap 注入），移过去即全绿。

## Q2 done_when 是否全部通过

- capability-sync.py 1483 行（< 1500 门禁），omo lint schemas 7/7、
  direct-omo-io PASS。
- capability 套件全绿（sync 129 + native receipt/inspection/trace 124）。

## Q3 与计划不符的事实

纯 move-only 计划执行中唯一偏差是常量归属：VERIFICATION_* 常量原在
capability-sync，下沉为 mesh 模块单一来源（capability-sync 顶部回导），
避免了循环导入。这是结构性改善而非偏差。

## Q4 净增减

- 净增：lib/capability_sync_mesh.py（437 行，验证投影单一来源）。
- 净减：capability-sync.py 1575 → 1483 行（god-module 错误门禁恢复绿）。
- 零行为变更：函数体逐字搬移，仅 import 接线。

## Q5 给下一个 owner

- `bin/capability-sync.py` 的 sibling 模块全部住在 `lib/`（LIB bootstrap
  在第 35 行）；新 helper 必须进 `lib/`，`bin/` 不在 import path。
- god-module 警戒线 600 行（warn）/1500 行（error）；capability-sync 仍在
  warn 区间（>600），后续成熟度窗口可继续按此模式拆解 admission/gateway 块。
