---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-144
status: archived
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-12
type: ephemeral
---

# BET-Y1Q4-T10-144 retro — A2 Resident Status Purity Truth Recovery

## What changed

- **WP-A2-CHILD**（Spec 1.0.0，历史关闭）：omo#152 交付 CQS——status
  改为 check_lock_state_only + _probe_ledger_once 纯读路径，删除
  check_and_recover 动态导入/重试/sleep；显式 recovery 走
  check_and_recover（别名保留）。
- **Spec 1.2.0 genesis NULL probe 修正**（omo#162 / 769e280a）：
  _probe_ledger_once 对 sequence-1 的 expected previous_hash 接受 NULL
  （对齐 LedgerBroker.verify_chain），消除生产账本恒报
  "chain broken" 的假阴性。
- **WP-A2-ROOT**（Spec 1.3.0，本轮）：omo gitlink 推进至 e15bc2b
  （含 769e280a 的 authoritative child main 后继），#3601 先行落地
  同一 gitlink；本轮补齐 root 侧 evidence 与 100/100 host canary。
- **canary 实测**：100/100 PASS（判定：JSON 可解析 + health ok/degraded
  + recovery_performed=false + observation_mode=read_only）。

## Q3 (打假)

- "显存/SQLite 副作用为零"由**代码结构 + 测试哨兵**证明（CQS 后
  status.py 中 check_and_recover/sleep 出现 0 次），非运行时探针。
- canary 首轮 0/100 是**判定脚本错**（DeprecationWarning 混入判定流 +
  tail -n +2 错位），status 本身 100% 正常——测量工具要先自证。
- spec last-reviewed 未来日期在 CI 时区下触发 unbaselined 红一次；
  spec 落库日期一律 UTC。
- worktree 生命周期与并发会话冲突两次（t132/t6-21 目录被清）——
  收账动作要及时，长跑 run 的 run.yaml 要在 worktree 存活期内消费。

## Q4 (遗留)

- 100/100 canary 的 launchd 定时化（每日纯度抽检）属后续运维 bet。
- value 轴保持 NOT_PROVEN（只读纯度不产生个人价值证据）。
