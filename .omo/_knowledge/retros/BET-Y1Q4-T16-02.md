---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T16-02
status: active
lifecycle: history
owner: governance-team
last-reviewed: '2026-09-14'
type: ephemeral
---

# BET-Y1Q4-T16-02 retro — A4 remote-hygiene cron registry parity

## Q1 实际耗时 vs appetite？

Appetite 是 0.5 天。声明面修复本身很小；主要成本用于隔离共享脏树、
确认 #3777 accepted binding、以及避免把历史登记缺口伪装成 known orphan。

## Q2 done_when 是否全部通过？

- Registry parity：本 lane 需要复跑并达到 `drift_count=0, orphan_count=0`。
- Unbound bootstrap waiver 与 retro：已按 #3777 后续治理面 lane 落盘。
- Ledger lint：#3777 后基线为 412 bets、13 tracks、无错误；本 lane 不修改
  Ledger，合并前仍需复跑。
- Gate-health：本 lane 通过 staged governance 检查；A1–A5 全局 canonical
  复核在后续收口执行。

## Q3 过程中发现的与 plan 不符的事实。

- PR #3752/#3754 已把 remote-hygiene job 安装到用户 crontab，并修正脚本名，
  但始终没有登记 scheduler registry；A4 失败是声明面孤儿，不是运行失败。
- Installed command 的 cron comment 是 scheduler comparison 的一部分，注册时
  不能只保留逻辑命令而丢掉注释后缀。
- 已安装运行已经采用 `fix-remotes.py`，因此本修复不需要也没有改变运行态。
- Docs binding 与 governance-state 必须分 lane；#3777 已完成 binding，本 lane
  只处理 registry、waiver 和 retro，避免混合写面。

## Q4 净增减。

- Changed files: 3。
- New executable logic: 0。
- New GaC rules: 0。
- Runtime behavior change: 0。
- Registry declarations: +1（仅补登已存在事实）。

## Q5 下一个认领本 track 的 agent 需要知道什么？

新增 cron 时必须同时登记 `.omo/cron/registry.yaml`，并保留真实安装行的
完整 command 与 comment；closeout 前运行 `scheduler-compile.py --check`。
A4 declaration parity 不能推出 A6/A7/A8/A9 admission，业务 value 仍为
`NOT_PROVEN`。
