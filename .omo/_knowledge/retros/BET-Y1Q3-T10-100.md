---
schema_version: retrospective/v1
lifecycle: history
owner: governance-team
created: 2026-08-30
last_updated: 2026-08-30
bet_id: BET-Y1Q3-T10-100
---

# BET-Y1Q3-T10-100 retrospective

## Q1. What was intended?

Move the two remaining learning executors out of Documents while preserving
their useful semantics behind one guarded Workspace owner.

## Q2. What happened?

The owner was implemented in `lib/documents_learning_control.py` and routed
through the existing `documents-domain-owner-job.py`. Root PR #2695 merged to
`origin/main` as `81e1f1cba3ec1f7661013059ad435f31687a1bf8`. The two source
files were moved with L4 preflight and hash-verified quarantine manifests to
the actual `/Users/xiamingxing/Workspace/runtime/quarantine` root. Active
Documents operation pointers were updated; no learning content was changed.
During closeout, an inherited T1-12 ledger contract missing its original
workflow/write-surface fields was restored from the immediately preceding
mainline snapshot; its completion/value evidence was not changed.
The closeout audit also refreshed the two stale T4-07 engineering digests to
the current tracked retro SHA, without changing its value verdict.

## Q3. What went well?

TDD caught the absent owner, system Python 3.9 caught the eager L4 import
compatibility defect, and the final tests proved fingerprint locking,
collision refusal, atomic frontmatter updates, and rollback. Both L4 scopes
are now runtime-free and the consumer audit remains zero forbidden/zero
unmatched. CI completed all required checks, including governance-verify.

## Q4. Surface accounting

Root implementation: 2 files, 725 insertions plus 14 routing lines; governance
registry/report/spec/retro surfaces were committed in separate lanes. Host
executioners moved: 2 regular files, 13708 bytes total; source content bytes
were preserved exactly in canonical Workspace quarantine. No permanent delete,
schedule, launchd, or client mutation occurred.

## Q5. What changes next?

`learning-runtime` remains `in_progress`: the next independent wave owns
`l4-kernel.sh`, `vault-healthcheck.sh`, daemon wrappers, and `.githooks`.
