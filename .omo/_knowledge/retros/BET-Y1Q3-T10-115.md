---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-115
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-05
type: ephemeral
---

# BET-Y1Q3-T10-115 retro — 署名 Diff 反向萃取与负样本硬过滤

## What changed

- **`omlxc/dataplane/hard_negative_miner.py`**（新）：difflib opcode 级
  `parse_signature_diff` → 规则启发式 `classify_hunk`（banned_phrase /
  verbose_trim / terminology_replace / fact_fix / precision_add 等 7 类
  语义意图）→ `mine_negatives`（≥2 次重复模式聚合为 HardNegativeRule）
  → `export_rules`（JSONL 规则库 `.omo/state/hard-negative-rules.jsonl`）。
- **`ecos/governance/diff_broker.py`**（新）：`check_draft`（命中即拒 +
  违规明细）与 `enforce`（MOF 动态约束钩子，违规 raise
  DIFF_BROKER_REJECTED，无豁免通道）——100% 拦截语义。
- 测试 10/10（omlxc 5：diff 解析/套话分类/真实 buffer 冒烟/聚合/导出；
  ecos 5：命中拒/干净过/enforce raise/空库 permissive/原始初稿 100% 拦截）。

## Q3 (打假)

- **真实 32 条 buffer 冒烟：0 条规则**（意图分布 fact_fix 80 /
  verbose_trim 17 / terminology_replace 3，无 ≥2 次重复套话模式）。
  done_when[1] 的拦截语义由 broker 单测证明（命中即拒，含原始初稿用例），
  真实命中数当前为 0 是数据特性——夏明星的署名修改以事实修正为主，
  套话类负样本需更长署名史积累。规则库空 = broker permissive（合法态）。
- 两个实现教训：① replace 型 hunk 的被删侧也要查套话（首版只查 delete
  导致分类漏）；② banned 聚合键须用命中的套话短语本身（hunk 整段含
  样本特定前缀，作键会导致同模式无法聚合）。
- ecos 侧测试对 omo 主仓的 omlxc 规则路径以显式参数注入，避免跨仓
  workspace 探测歧义。

## Q4 (遗留)

- 语义级矛盾（"应 A" vs "应 B"）与风格规则（句长/段构）不在规则
  启发式覆盖面。
- 规则库人工复核界面由 T10-116 工作台承接。
