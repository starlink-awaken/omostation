---
bet_id: BET-Y2Q2-T10-155
created: '2026-09-22'
type: retrospective
---

# BET-Y2Q2-T10-155 复盘

## Q1 实际耗时 vs appetite？

Implementation, live projection, and verification completed within the four-hour appetite.

## Q2 done_when 是否全部通过？

- PASS — focused workspace-root contract tests: 10/10.
- PASS — existing workflow tests: 8/8; existing all-collector tests: 25/25.
- PASS — the fresh dashboard projection reported `workflow=OK`,
  `scheduler=OK`, `portfolio=OK`, and every required cockpit source OK. There
  were zero failed sources.
- PASS — a fresh A9 gate calculation returned `PASS`; ASD was complete and the
  oldest required cockpit source success was fresh.
- PASS — ledger lint and the full local governance gate passed.

The dashboard implementation commit was
`09d086c6c94b68387150e1769359538328e35fec`, tagged
`bet/BET-Y2Q2-T10-155-dashboard-20260922T043518Z`, and delivered by dashboard
PR #1 with merge SHA `a813ac34c524be346fad7b8da9fc86f7a0f6389f`.

Operational evidence:

- `live_canary`: managed-Python dashboard refresh published with
  `result_state=observed` and `failed_sources=[]`; workflow and scheduler were
  both `OK`.
- `fresh_receipt`: root Panorama regenerated at
  `2026-09-22T04:33:27.711974+00:00`; its fresh A9 verdict was `PASS`.
- `replay`: the same collector patch was replayed onto dashboard
  `origin/main` at `c704c2bab98d47d77ceba5a63ed56256f04d6dbb`; all three test
  suites passed there before publication.
- `cleanup`: the separately approved T5-02 orphan closeout released only its
  three stale path locks; the live T5-03 `project:` and `root-gate` locks
  remained intact, and workflow observer returned `ok=true / findings=[]`.

## Q3 过程中发现的与 plan 不符的事实（打假）？

- The first dry-run exposed an additional contract gap: scheduler previously
  returned an OBSERVED-like payload even for `ok=false`. The implementation now
  rejects non-dict and `ok=false` responses.
- A dashboard local-main transport contained four unrelated UI commits. They
  were not used. The authorized patch was replayed onto dashboard
  `origin/main`, so the implementation PR contained only the three authorized
  files.
- The first dashboard refresh used system Python 3.9 and failed the portfolio
  source on `datetime.UTC`. A managed Python refresh repaired the projection
  without weakening the source contract; this environment issue was not
  attributed to the collector change.

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？

Dashboard implementation: 218 additions / 25 deletions across two collector
modules and one focused test. Root governance surfaces added one accepted
Spec binding update and this retro. No GaC rule, ADR, root script, CI workflow,
or second control-plane component was added.

## Q5 下一个认领本 track 的 agent 需要知道什么？

Use the canonical OMO workspace root and its managed `pyyaml` runtime for
workflow and scheduler probes. Keep fail-closed behavior for non-dict, schema
mismatch, non-zero, and `ok=false` results. Refresh the dashboard with a
Python runtime that provides `datetime.UTC`; do not infer source failure from a
Python 3.9 host limitation. Value remains `NOT_PROVEN`; no user-value evidence
was collected or backfilled.
