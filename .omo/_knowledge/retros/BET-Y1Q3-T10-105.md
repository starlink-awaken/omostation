---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-105
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-05
type: ephemeral
---

# BET-Y1Q3-T10-105 retro — 个人文风 LoRA 管线工程交付

## What changed

本阶段完成管线工程（真实执行路径，去除模拟）：

1. **omlxc `experience_replay.py`**：新增 `dispatch_distill`（本地 MLX 训练
   `mlx_lm.lora` → mesh 漫游路由（复用 RoamingComputeRouter，目标如 Mac mini M4）
   → 诚实失败三级派发）、`adapter_status`/`adapter_dir`（适配层生命周期）、
   `evaluate_alignment`（纯 Python ROUGE-L，零新依赖，含 base 零分守卫）。
2. **cockpit `spine.py`**：`sign` 在 broker 成功后真实落盘
   `lora-replay-buffer.jsonl`（经 `_omlxc_python` 解耦调用，显示域内样本计数）；
   `distill` 从模拟应答改为真实派发（dispatched/routed/insufficient_samples/
   needs_mlx 四态，绝不模拟成功）；`draft` 自动检测并加载
   `adapter-xiamingxing-v1`。
3. **测试**：omlxc 9 个（含 mock MLX 本地训练路径、mesh 路由路径、诚实失败
   路径）、cockpit 2 个（distill 无 omlxc env 时返回 1 的新契约）。
4. 台账 write_surfaces 扩展 `projects/cockpit/tests/test_spine_cli.py`
   （distill 行为契约变化需同步测试断言）。

## Q3 (发现与偏差)

- `spine draft --adapter` 参数无法注册：argparse 定义在
  `cockpit/_subcommands.py`（本 bet write_surfaces 之外）。当前用
  `getattr(args, "adapter", 默认名)` 兼容，功能完整但 CLI 不可覆盖适配层名。
  后续 bet 扩写面后补参数注册。
- 本机为 Apple M5 Max，非 done_when 指定的 Mac mini M4；MLX 本地路径在 M5 Max
  机制等价可跑，"Mac mini M4 上完成训练" 一项待 mesh 节点实跑或复盘放宽论证。

## Q4 (遗留)

- done_when[0]（≥30 条真实署名样本）与 done_when[2]（对齐度提升 ≥25%）需要
  夏明星真实使用积累样本后才能验证——本 bet 保持 candidate，不收账。
- mesh 侧 Mac mini M4 的 `bos://compute/omlxc/lora` 服务端点未部署。

## Evidence

- omlxc 9/9 tests pass (`uv run pytest tests/unit/test_experience_replay.py`)
- cockpit 2/2 tests pass (`pytest tests/test_spine_cli.py`)，ruff clean
- omlxc commit 7136574（已推 main）；cockpit commit fca4c03（rebase #135 后推 main）
