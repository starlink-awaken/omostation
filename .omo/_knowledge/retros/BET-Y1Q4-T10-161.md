---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-161
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-12
type: ephemeral
---

# BET-Y1Q4-T10-161 retro — Git 子模块稳定性与 remote 配置完整性防护

## What changed

- **`bin/gac/gac-worktree.sh`**：新增 `guard-submodules [--fix]` 守卫——
  ① gitlink 新鲜度：`submodule status` 检出 `+/−/U` 条目，`--fix` 时本地
  无网络 `submodule update --init --no-fetch` 对齐 pin（只动子模块工作树,
  不改根指针）; ② remote 完整性：主仓 remote URL 落入 `.gitmodules`
  子仓 URL 集合即告警（只告警不改写, 守 non_goals）。claim 的
  `SKIP_SUBMODULE_INIT=1` 快速路径自动补偿调用。
- **`.githooks/post-checkout`**：在 hook-runner 委托前内联调用守卫
  （`--fix`, 输出抑制, `|| true` 恒不阻断）——覆盖 `git worktree add`
  绕过 claim 脚本的路径; `GAC_SKIP_POST_CHEKOUT_GUARD=1` 可跳过。
- **`AGENTS.md` §6**：固化"worktree 子模块与 remote 完整性"协议
  （守卫行为、污染特征、修复命令）。
- **附带修复（PR #3680）**：铸造缺陷——T10-161 无列表项边界（键值粘入
  T10-159 mapping 被 PyYAML last-wins 遮蔽）且无 accepted_specifications;
  已插入边界成独立 bet（账本 406→407）并绑定本 bet 真实 spec。

## 事故映射（为什么这两个守卫）

1. 本 session 实证 2 次 cockpit-ui 指针回退（worktree 创建后未 init）;
   T7-04 交付期间又发生 4 次（family-hub/omo/cockpit-ui/aetherforge 轮番,
   main 在 CI 期间前进 + worktree 基点陈旧）。
2. 主 checkout origin URL 被并发会话改写为 cockpit-ui 仓（remote 污染）,
   T8-24D 收账排查 ~2h 后才定位; 工具链此前对该类污染零观测。

## 守卫实测（本 session 双场景验证）

| 场景 | 结果 |
|------|------|
| 子模块 checkout 至 pin~1（模拟陈旧）→ `guard-submodules --fix` | 检出 `+c4ed708` ≠ pin → 本地无网络修复 → 回到 pin `0377078` ✓ |
| 临时仓 origin = 子仓 URL（模拟污染）→ `guard-submodules` | 🚨 告警 + 修复命令, exit 0 ✓ |
| 全同步环境 | 无输出, exit 0, 耗时 0.35s ✓ |

## Done when 逐条

- root aetherforge gitlink = `79842ed097b7…`：✅ 已由 #3664 满足
  （`git ls-tree origin/main projects/aetherforge` 核验）。
- SHA 在 omostation-aetherforge origin/main 可达：✅（#3664 合并即满足）。
- gates 在指针 bump PR 上通过：✅ 本 PR CI（gac-gate 等）+ 本地 verify。

## Verify 实测（run 20260912T094101Z-project-code-change-d7b1ac29）

- `python3 bin/gac/check-submodule-pointer-drift.py --json` → exit 0
- `python3 bin/gac/check-sfop-slots.py --json` → exit 0
- `bash bin/gac/gac-worktree.sh guard-submodules --fix` → exit 0（0.35s）

## Rollback

revert 单 PR 即可（守卫为 advisory 恒 exit 0, 不阻断任何现有流程;
不改 remote/根指针/子模块源码）。

## Lessons

- 账本条目缺列表项边界时 PyYAML last-wins 会把两个 bet 合成影子 dict,
  lint 不报错——铸造/审计工具应校验 `bets` 序列长度 vs `total_bets` 计数。
- 改 tracked 文件后不要 `git reset --hard`（本次冲掉未提交守卫改动, 重做一遍）;
  未提交工作先 stash 或提交到 side 分支。
