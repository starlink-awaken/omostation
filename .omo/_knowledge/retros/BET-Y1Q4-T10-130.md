---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-130
status: archived
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-06
type: ephemeral
---

# BET-Y1Q4-T10-130 retro — 仓库健康度可视化周报

## What changed

- **`bin/gac/repo-health-metrics.py`**（新）：`collect`（分支/tags/
  worktree/松散对象/悬空跟踪/gitlink 偏差采集，`git fetch --prune`
  后比对 ls-remote 判定悬空跟踪）、`--snapshot`（JSONL 历史采样）、
  `--report`（Markdown 周报：当期指标 + 趋势箭头 + 告警区）、
  告警阈值（松散对象>1000/悬空>5/worktree>25/gitlink 偏差>3）。
- 首期周报 `docs/repository-health.md` 入库 + 首次采样。
- 测试 6/6（采集形态/趋势箭头/告警阈值/报告结构/快照往返/CLI 端到端），
  ruff clean。

## Q3 (打假)

- 首期采样即触发 1 条告警：**松散对象 4307 > 1000**（周报已标红）——
  真实发现，处置属 T10-127 的 gc cron（本 bet 只观测告警）。
- 悬空跟踪判定用 `fetch --prune` + `ls-remote` 双向比对（网络依赖，
  timeout 300s）；无网络环境此指标会偏高（prune 前的 stale 引用）。
- binding PR 的 CI 曾红：并发方 ledger 瘦身（T10-03 archive 合并读）
  落地瞬间 base 快照与 CI 读时序错开——合并最新 main 后消失。
  **race 类 CI 红先合并 main 复现再排查**，不要盲目修。

## Q4 (遗留)

- 周报趋势需 ≥2 次采样（首期箭头为 —）；launchd 定时由人工安装
  （脚本头部片段）。
- 悬空跟踪分支的清理（`branch -d`/`remote prune`）属 T10-128/129。
