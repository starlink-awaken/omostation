---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-133
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-07
type: ephemeral
---

# BET-Y1Q4-T10-133 retro — Git Worktree 物理沙箱与时光机回滚

## Q1 实际耗时 vs appetite？

appetite 2-3 days；实际约 1 天（含 spec+binding 链门）。并行 agent 已交付 T10-132（hydration）作为依赖，本 bet 独立实现。

## Q2 done_when 是否全部通过？

- ✅ `resident-sandbox-timemachine.sh` 交付：create/run/rollback/--help 四子命令，`--help` exit 0
- ✅ `sandbox_driver.py` 交付：SandboxDriver 三阶段（create→run→outcome），失败自动回滚
- ✅ 试错失败回滚后主工作区 git status 干净度 100%（集成测试实测 count 前后一致）
- ✅ 集成测试 7/7 PASS（--help / 隔离性 / 回滚干净度 / push 拦截）
- ⚠️ bet-ledger lint 全绿被 **T10-128 既有问题**（spec status=active 非 accepted + completion_evidence 缺 axes）阻断——非本 bet 引入，不修（越权 D3），记录备查

## Q3 过程中发现的与 plan 不符的事实（打假）？

1. **macOS 无 GNU `timeout` 命令**——`timeout git worktree add` 直接失败，首版 create 全挂。改用 python3 子进程 + timeout 参数（跨平台）。
2. **写面路径陷阱**：spec 的 write_surface `projects/omo/src/omo/resident/sandbox_driver.py` 在 PASW 隔离下实际改 `.subtrees/omo/`（子模块隔离 worktree），直接改 projects/omo（detached）会丢——先误放再纠正。
3. **全局模式匹配插入 binding 的教训**：修 T10-133 binding 缩进时用"前一行是 circuit_breaker"匹配，**误覆盖了 T6-22 的 binding**（T6-22 的 accepted_specifications 前一行也是 circuit_breaker）。已恢复 T6-22 正确值。教训：**台账修改必须块内定位（按 id 找块边界），禁止全局模式匹配**。
4. **T10-134 撞车**：claim 后才发现并行 agent 已交付（PR #3342 合并 + #3351 收尾中）。已放弃 T10-134，改为 T10-133。教训：claim 前先查 main 上 write_surface 是否已实现 + 是否有相关 PR（PITFALL-GAT-006）。

## Q4 净增减（表面积记账）

- 新增文件 3：`bin/gac/resident-sandbox-timemachine.sh`（~150 行）、`projects/omo/src/omo/resident/sandbox_driver.py`（~130 行）、`tests/integration/test-sandbox-timemachine.sh`（~75 行）
- 修改 2：ledger（binding）、spec（新增）
- 无删除。净增 ~360 行 + 3 文件——**P0 新能力（沙箱隔离）必要新增**，未配减法；与 T10-128/T10-132（命名空间隔离/hydration）同属持久化 agent 基建族。

## Q5 下一个认领本 track 的 agent 需要知道什么？

- **沙箱脚本依赖 macOS 兼容**：不要用 GNU `timeout`，用 python3 子进程超时。
- **子模块写面走 PASW**：改 projects/omo 的文件要确认是否在 `.subtrees/omo/` 隔离分支（`git -C projects/omo worktree list` / `git -C .subtrees/omo branch --show-current`），提交走子模块分支 + 主仓 bump 指针。
- **台账 binding 修改纪律**：按 bet id 块内定位，禁止全局字符串/模式匹配（T6-22 binding 被误覆盖事故）。
- **T10-128 遗留**：spec status 需改 accepted + completion_evidence 需补 axes——另一个 agent 的 done bet 未规范化，后续可修（需先 claim 该 bet 或经人类确认）。
- **沙箱成功场景保留 worktree**（供复核），失败自动回滚——`execute()` 全链路已封装，直接用。
