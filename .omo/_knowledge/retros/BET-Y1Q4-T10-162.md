---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-162
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-12
type: ephemeral
---

# BET-Y1Q4-T10-162 retro — omlxc gitlink 指针收账 (inventory-baseline reset)

## What changed

- **子仓 omostation-omlxc PR #73**（已合并, commit `c030788e8792…`）：新增
  `omlxc nodes reset-inventory-baseline <node_id> <backend_id>`——修复
  inventory_drop 高水位只升不降导致的三处永久假警（本地模型目录有意
  收缩后 stale baseline 警告无法消除）。已对三个真实 flagged backend
  实测, `omlxc status` 归零。
- **主仓 PR #3675**（已合并, commit `7a831efaf29f…`）：root
  `projects/omlxc` gitlink bump 至 `c030788e8792…`。

## 本 run 的职责边界

指针 bump 工程交付已由并发会话完成（#3675 合并, 门禁全绿）。本 run 按
PITFALL-GAT-006 不重做 bump, 只做独立核验与台账收账：

- done_when①：`git ls-tree origin/main projects/omlxc` = `c030788e8792…` ✓
- done_when②：SHA 在 omostation-omlxc origin/main 可达（merge-base 判定）✓
- done_when③：#3675 合并即代表其 CI 门禁通过 ✓

## Verify 实测（2026-09-12, run 20260912T091216Z-submodule-pointer-close-8229b83f）

| 命令 | 结果 |
|------|------|
| `python3 bin/gac/check-submodule-pointer-drift.py --json` | exit 0（diverged: 0, behind: 2） |
| `make governance-release-gate` | exit 0（submodule-reachability: PASS, 16 gitlinks, source=head） |
| `python3 bin/plan/bet-ledger.py lint` | OK, 406 bets, no errors |

## Rollback

revert 主仓 `projects/omlxc` gitlink 至父值 `e92317572eef…`（单行回退,
无其他状态依赖）; 子仓 PR #73 无需回退（独立可用的新命令）。

## Lessons

- 指针收账类 bet 的三步核验（ls-tree 等值 / child 可达 / bump PR 门禁）
  已成稳定模式, 可考虑抽成 bin 工具减少每个 closeout 的手工核验成本。
