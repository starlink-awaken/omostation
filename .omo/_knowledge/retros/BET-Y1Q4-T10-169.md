---
schema: md/v1
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-25
type: retro
title: BET-Y1Q4-T10-169 复盘
---

# BET-Y1Q4-T10-169 复盘

## Q1 实际耗时 vs appetite？超出比例？
诊断使用 Actions run 35418257725、workflow 元数据、根 tree 和根依赖文件。
实际范围是一页 spec、一份根级 Dependabot 配置、Ledger 收口和本复盘，
符合 2 小时以内的小修复 appetite。

## Q2 done_when 是否全部通过？哪条没过，为什么？
根配置只保留 `/projects` 之外的根 checkout `/`；YAML 结构断言通过。
run 日志明确显示 `/projects/aetherforge, /projects/knowledge/kairon,
/projects/omo not found`，根 tree 中没有这三个 checkout 目录，因此根配置
不得再指向它们。Ledger lint 与完整 recursive checkout CI 由交付 PR 验证。

## Q3 过程中发现的与 plan 不符的事实（打假）
根仓所有本地和远端历史都不存在 `.github/dependabot.yml|yaml`，但 GitHub 的
`dynamic/dependabot/dependabot-updates` workflow 仍是 active。这说明故障配置
不来自当前默认分支文件；新增根级配置是对平台当前生效配置的显式覆盖。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？（贴 surface 输出）
新增 `.github/dependabot.yml` 10 行、accepted spec 1 份、retro 1 份；Ledger
新增 1 个 BET 条目并登记 spec。未新增代码、测试、GaC 规则、ADR、脚本、
子模块内容或根 gitlink 变更。

## Q5 下一个认领本 track 的 agent 需要知道什么？
下一次 run 若仍报同样目录，应先比较 GitHub 平台配置或 dynamic workflow 的
job inputs，而不是在根仓重复添加第二个 `updates` 条目。子仓依赖更新应分别
在子仓仓库管理，或通过专门的 child-repo workflow，不得恢复根 checkout 里的
`projects/**` directory。
