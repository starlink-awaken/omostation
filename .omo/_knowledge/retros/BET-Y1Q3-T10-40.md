# BET-Y1Q3-T10-40 retro

The predictor was a pure data-producing capability, but its schedule wrote a
dated report into the Documents runtime tree. Preserving the four forecast
categories in a Workspace evidence envelope removes that write without hiding
the forecast's attention status.

The replacement intentionally returns exit 1 for an attention forecast; it does
not convert unresolved business risk into a false healthy result.

The governed cutover used `accepted-20260905` and replaced exactly one legacy
Documents cron line. The post-cutover smoke preserved the 15-file report-tree
inventory and digest, proving the owner reads Documents but writes only the
Workspace evidence target. The crontab backup, after snapshot, and smoke
receipts are retained under the governed `.omo/evidence/` run directory for
rollback and audit.
