---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-159
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-12
type: ephemeral
---

# BET-Y1Q4-T10-159 retro — aetherforge gitlink 指针收账 (Anthropic Messages API)

## What changed

- **子仓 omostation-aetherforge PR #72**（已合并, commit `79842ed097b7…`）：
  Anthropic Messages API 支持——解除 Claude Code / Anthropic SDK 客户端对
  本地网关的阻断。
- **主仓 PR #3664**（已合并, commit `729f06a32d…`）：root
  `projects/aetherforge` gitlink bump 至 `79842ed097b7…`。

## 本 run 的职责边界

指针 bump 工程交付已由并发会话完成（#3664 合并, 门禁全绿）。本 run 按
PITFALL-GAT-006 不重做 bump, 只做独立核验与台账收账：

- done_when①：`git ls-tree origin/main projects/aetherforge` =
  `79842ed097b792cd2a856d62f8d08d4bf6f1a3de` ✓
- done_when②：SHA 在 omostation-aetherforge origin/main 可达
  （merge-base 判定）✓
- done_when③：#3664 合并即代表其 CI 门禁通过 ✓

## Verify 实测（2026-09-12, run 20260912T113202Z-submodule-pointer-close-b1a668c5）

| 命令 | 结果 |
|------|------|
| `python3 bin/gac/check-submodule-pointer-drift.py --json` | exit 0 |
| `make governance-release-gate` | exit 0（submodule-reachability: PASS, 16 gitlinks, source=head） |
| `python3 bin/plan/bet-ledger.py lint` | OK, 407 bets, no errors |

## Rollback

revert 主仓 `projects/aetherforge` gitlink 至父值 `452f0db`（单行回退,
无其他状态依赖）; 子仓 PR #72 无需回退（独立可用的 API 支持）。

## Lessons

- 指针收账三步核验模式（ls-tree 等值 / child 可达 / bump PR 门禁）本轮
  第 4 次复用（T10-158/162/159 + T8-24D）, 抽成 bin 工具的收益已明确。
