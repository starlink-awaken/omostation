---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-123
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-07
type: ephemeral
---

# BET-Y1Q4-T10-123 retro — Documents 归档族冻结与注册表收口

## What changed

- **冻结清单**：5 大内容归档族（274,102 文件 / ~58.8GB）per-file SHA-256
  索引（runtime 平面 106MB）+ 分族聚合摘要（docs/ 1KB）。
- **registry 收口**：6 族标 verified（5 content_archive + dashboard-app
  migrate，后者以 T10-122 parity receipt 为证），附 terminal evidence
  五件套（consumer_scan/rollback_ref/source_fingerprint/target_fingerprint/
  verified_at）。non_terminal **13 → 7**。
- **rollback receipt**：`documents-archive-rollback-receipt.md`（检查器
  要求 rollback_ref 解析为真实文件）。

## Q3 (打假)

- 首版冻结清单逐文件带 size 元数据 = 119MB，不可入库；重构为
  "runtime 全索引 + docs 摘要"两层。
- done_when 说 non_terminal "减少至少 7 个"——实测 13→7 只减 6。
  第 7 个（family-dashboard-app）以 T10-122 parity receipt 补齐后
  达标——**起点口径**（13 含 dashboard-app）与终点（7）的差值算术
  需要在报告里写清楚。
- 检查器读 `evidence` 字段而非 `progress_evidence`——首版字段名
  错误导致 terminal 校验全红。旧家族（cc-switch 等）用的正是 `evidence`。
- rollback_ref 必须解析为真实文件（检查器 is_file 校验）——散文式
  描述不行，必须落 receipt 文档。
- DeepTutor（toolbox-staging）物理重定位未完成（源还在
  `@学习进化/_external/`）——该族保持 pending，不虚标。

## Q4 (遗留)

- 剩余 7 族：toolbox-staging 物理重定位、zotero 保留策略、3 个
  delegate 证据链、public-runtime job 接线、root-oneoff 清理。
- 冻结的 274k 文件仍是物理存在——只读声明不阻止磁盘占用；gc/清理
  属 T10-127 cron 系列。
