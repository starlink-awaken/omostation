---
schema_version: retro/v1
lifecycle: history
type: bet-retrospective
owner: governance-team
created: 2026-08-29
last_updated: 2026-08-29
bet_id: BET-Y1Q3-T10-83
---

# BET-Y1Q3-T10-83 retrospective

## Q1. What was intended?

Remove the root-level generic tools runtime copies from Documents without
changing content, restoring a missing runtime target, or claiming family-level
completion.

## Q2. What happened?

Exactly four regular files were moved to protected Workspace quarantine. Their
bytes, modes, and hashes were retained in a rollback manifest. Three files
match the already quarantined domain copies; the root-only `domain_controller`
implementation was preserved as its own rollback entry. No active consumer was
found for the moved copies.

## Q3. What was discovered?

The root tools directory held the canonical-looking duplicates for generic
controller/extract/predictor behavior, but the current consumer audit has no
active executor bound to these Documents paths. The adjacent root `_control`
status content remains in Documents.

## Q4. Surface accounting

Documents lost four regular runtime directory entries and Workspace gained one
four-file quarantine package plus manifest (`19848` bytes). No content,
contract, cache, schedule, client, public-runtime target, or family status
changed. Root progress evidence gained one completed sub-scope.

The required D2 tracked-surface receipt at closeout was: `src_loc=949403`
(`+222991` from the 2026-08 baseline), `test_loc=481392` (`+130538`),
`src_files=4218`, `test_files=2320`, `adr_total=393`, `gac_required=28`,
`bin_scripts=710`, `standards=56`, and `collab_scenarios=5`. Since 2026-08,
the project numstat net was `_root +207459`, `cockpit +31099`, `agora +9165`,
`omo +42280`, `ecos +30281`, `runtime +21838`, `omlxc +25496`,
`l4-kernel +2969`, `aetherforge +213`, and `metaos +29`; `test_loc` did not
decline.

## Q5. What should the next agent know?

Keep the manifest protected and use hash/mode checks for rollback. The
`work-runtime` family remains pending. The next candidates are the remaining
root/control tool copies or other explicitly classified regular runtime files;
do not mix specialized `_scripts` or content archives into this transaction.

## 批量收口授权补记 (2026-08-30)

principal 授权: "授权T10隔离批量执行" (2026-08-30 会话)。隔离事务与消费证据已由交付 PR 落 main (报告见 docs/reports/); 本补记授权台账批量收口 (矩阵 + complete)。
