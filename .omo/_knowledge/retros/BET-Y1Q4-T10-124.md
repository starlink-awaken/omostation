---
bet: BET-Y1Q4-T10-124
status: completed
date: 2026-09-06
run_id: 20260906T090049Z-project-code-change-b7dc9e94
---

# Retro: BET-Y1Q4-T10-124 — Runtime Jobs Parity & Cadence

## What shipped
- `_validate_runtime_learning_job()` in `documents_domain_jobs.py` — validates `evidence_relative_path`/`evidence_schema` for `runtime-learning` owner jobs
- `documents-jobs-scheduler.py` — launchd plist generator for 7 manual runtime jobs
- 6 unit tests covering schema acceptance and rejection
- Domain project check now returns `ok=true` for all 12 domains + 7 jobs

## What was learned
- The `runtime-learning` owner jobs (`documents-learning-decay`, `documents-learning-orphans`) were defined in the project registry with `evidence_relative_path` and `evidence_schema` fields, but the validator had no branch for `runtime-learning` owner — they fell through to the manifest validator which required `evidence_path` instead.
- The fix was surgical: add a field set constant + validator function + dispatch branch. Total diff: ~80 lines.

## What was hard
- Needed symlink workarounds for `projects/ecos` and `projects/l4-kernel` submodules (not initialized in the worktree)
- The `start` command requires a valid `accepted_specifications` binding with correct frontmatter schema

## Action items
- [ ] Consider converting the 7 `schedule: manual` jobs to `schedule: daily` in the registry
- [ ] Wire `documents-jobs-scheduler.py --install` into a post-merge hook or CI step
