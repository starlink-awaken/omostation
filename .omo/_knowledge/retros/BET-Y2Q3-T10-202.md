---
status: active
lifecycle: history
owner: governance-team
created: 2026-09-24
last-reviewed: 2026-09-25
title: BET-Y2Q3-T10-202 retrospective
type: doc
bet_id: BET-Y2Q3-T10-202
---

# BET-Y2Q3-T10-202 retrospective

## Q1. What was intended?

修掉 ADR-0443 事故→规则管道的两个断头，让它们声称的闭环真的闭合：

1. **队列耐久性（G1–G4）**：`.omo/_delivery/rule-drafts/` 入仓、晋升有独立扫描入口
   （`promote-drafts`）、`stats`/`check` 报出逾期缺口（不改 `check` 退出码）、
   `feed_from_escapes` 不再跨类别给 `times_encountered` 加分。
2. **start 门对称豁免（G5，principal 明确纳入范围）**：`chain_bind.start_requires_bet`
   对治理演进 workflow 采用与 `evaluate_closeout` 完全相同的豁免谓词，
   从根因上消灭 70 条 `AGCP_REQUIREMENT_ITERATION_GATE=0` 豁免的需求。

约束：零 gate 豁免、零 waiver 文件、草案只能由 promoter 本身生成、不碰
`governance-checks.yaml`、不改共享门禁退出码。

## Q2. What happened?

两条目标都达成，且全部由实测驱动：

- 队列从"目录不存在"变成 3 份入库草案（`CR-PITFALL-COO-003` / `-GAT-004` / `-GAT-005`），
  `promote-drafts` 幂等（第二次 `created: []`），`stats` 报 `overdue_rule_drafts: []`。
- `check` 退出码与 `origin/main` 版本在同一份库上逐字段对拍：两者 `exit=0`、`problems` 相同，
  新实现只多一个只读 `rule_drafts` 段落。
- G5 后 `bin/plan/chain-bind-check.py self-check` 13 个 fixture 全绿，
  `tests/test_chain_bind.py` + `tests/unit/gac/test_chain_bind_start_gate.py` +
  `tests/unit/gac/test_error_knowledge_rule_drafts.py` + `test_error_knowledge_recall.py` 合计 37 passed
  （I8 收敛两道锁时新增 3 条：AST 对拍两份白名单 + 两条参数化的"草案可提交 / `_delivery` 其余路径仍被拦"）。
- 本交付 `start` 绑定真实 bet、未设任何 `AGCP_*` env、未新增 waiver 文件。

## Q3. What changed during implementation?

三处初始假设被实测推翻，都回写了 spec（不藏）：

1. **spec 说"两个调用点各自传 workspace"** —— 实测两个调用点都从 `<root>/bin/plan/chain_bind.py`
   动态加载，`DEFAULT_WORKSPACE = Path(__file__).resolve().parents[2]` 已经天然拿到正确工作树，
   调用点无需改动；`projects/omo` 侧还是子模块，不该由主仓交付顺手改。
2. **契约反转的落点比预想多两处** —— `chain-bind-check.py self-check` 与 `tests/test_chain_bind.py`
   原本**故意**钉住"start 更严"的旧契约。只改 `chain_bind.py` 会让 13 个 fixture 直接红。
   代价：`write_surfaces` 从 11 扩到 15，而 `refresh-packet` 对"自己改台账的 bet"结构不可用
   （它拿工作区字节比 `origin/main`）→ 只能把 run `20260924T233640Z-…-61f70aad` 以 `failed`
   关闭并注明替代关系，重开 `20260924T234703Z-…-19edbaca` 才能合法 claim（发现 I7）。
3. **`check --json` 被我的"只读报告"打断** —— 追加的人读提示行混进 JSON 之后，
   `gac-local-gate` 的解析会炸。测试先失败、随后把提示限定在非 `--json` 分支。
   "只读报告"和"不污染 stdout 契约"是两件事，后者才是真约束。

另有一项原计划"只登记不修改"的改为随 G5 一并做掉：`bin/agent-workflow.py` 的 start 拦截提示
只列 `observer-audit` 与 ENV 豁免 —— G5 之后留着，等于把下一个 agent 又推回去签豁免。

## Q4. Lessons

- **"门的不对称"要用 waiver 计数来定价**，而不是用感觉。136 份 waiver 里 70 份是同一行 env，
  这个数字比任何形容词都更清楚地说明了 `start` 与 `closeout` 为什么必须同一条谓词。
- **豁免只能沿用已经存在的那一条**，不能新写一条更宽的。G5 的价值不在于"放行变多"，
  而在于两个门现在可以被同一份测试同时钉住（`test_start_and_closeout_agree_on_the_exempt_set`）。
- **管道的缺陷常在末端**：阈值、匹配、记录都工作了一年，而产物落在一个被 gitignore 的目录里、
  且没有任何地方报"该有而没有"。加一个 `overdue` 报告比加新机制便宜得多。
- **死掉的 CLI 面比缺功能更坏**：`confirm`/`reject` 有解析器、没 handler、exit 0，
  任何脚本都会以为草案被人审处置过了。显式 `NOT_IMPLEMENTED` 退 2 才是诚实的。
- **自指型 bet（改台账的 bet）必须一次算全改动面**，因为 refresh 路径对它结构不可用。
  这条已经写进 spec 验收第 6 条，也是 I7 交给 principal 的决策点。
- **同一个故障可能有两道互不相识的锁**。`rule-drafts/` 看不见的最初诊断是"被 gitignore"，
  改完 `.gitignore` 后 commit 仍被拦：`check-runtime-artifacts.py` 另有一份 `.omo/_delivery/`
  黑名单。而它的 `WHITELIST_PREFIXES` 早就为三个同构目录存在——那三个目录能入仓从来不只靠
  `.gitignore`。报"入仓"时要报全部锁，修一处等于没修（I8）。
- **packet 把 spec digest 当冻结输入**，所以活文档型交付的合法顺序是
  「spec 定稿 → 台账 digest → start → 之后只改代码」。start 之后再动 spec 一个标点，
  全部 claim 都会被 `WORK_PACKET_SOURCE_DRIFT` 拒绝，重开 run 没有第二条路（I7 第二型）。
- **台账的 `meta.total_bets` 是派生值，冲突"没报"就是它说谎的方式**。rebase 时 `bets` 尾部两侧
  各插一条（main 的 `BET-Y2Q3-T9-01` 与本 bet）冲突，而 `total_bets` 双方写的都是 `459`
  （各自从 458 加一）→ git 判定同一处同一改，合并后实际 460 条、声明 459 条，且没有任何残留标记。
  判据只能是"按合并后的事实重算并断言 `len(bets) == total_bets`"，不是"有没有 CONFLICT 行"（I9）。
- **留给 principal 的没有动**：`>=3` 词阈值、`PITFALL-GAT-004` 的证据质量（symptom 是一段
  `[PASS]` 日志、标题写 `(8x)` 而计数 46、`last_confirmed` 停在 2026-08-30）、run record 是否入仓、
  H2 的 pre-rebase 冲突、I1 的 closeout `ok` 语义、30 条价值记录、weekly-review。

## Q5. Closeout evidence（2026-09-25，本小节即收据）

本 bet 的 17 条 `write_surfaces` 里没有 `docs/reports`（台账内另有 113 条 bet 声明了它），
所以收案证据只能落在同一 bet 已声明的 surface 之内。实测：向 run
`20260925T022515Z-governance-audit-48aff1d3` claim
`docs/reports/2026-09-25-rule-drafts-durability-closeout.md` →
`WORK_PACKET_SCOPE_MISMATCH`（探测不改 run：`status: active` / claims 17 /
`updated_at` 仍是 `2026-09-25T02:27:07Z`）。而"事后往台账补 surface"这条路被
`validate_work_packet_run` 封死——它用 `prepare_bet_execution` 从台账+spec 重建 packet 并比对
hash，台账一动就是 `WORK_PACKET_SOURCE_DRIFT`（见发现 I7、I12）。

### 交付身份（逐项可复测）

| 键 | 值 |
|---|---|
| bet | `BET-Y2Q3-T10-202`（appetite 0.5 day） |
| spec | `docs/superpowers/specs/2026-09-24-rule-drafts-queue-durability-design.md` @ `sha256:e9ec0a53ff66835c3c48ee71bbeb98a13b3348a2e448b7ce5acb01f125cfd65c`（2026-09-25 复测与台账绑定值一致） |
| run | `20260925T022515Z-governance-audit-48aff1d3`：17 claims、零 `AGCP_*` env、零新增 waiver 文件 |
| PR | #4313，`state=MERGED`，`mergedAt=2026-09-25T03:14:43Z` |
| merge commit | `f122c9794a981d824bc0d5977afb1db7fd099477`（`git diff --stat f122c9794^ f122c9794` = 18 files, +1136 / −26） |
| CI | `gh pr checks 4313` → 0 fail（26 pass + 4 skipping） |

### engineering — VERIFIED

以下都是 2026-09-25 在 `origin/main@a7a3df889`（含本交付）之上重跑的，不是引用交付当天的日志：

| 检查 | 命令 | 实测 |
|---|---|---|
| 4 文件回归 | `python3 -m pytest tests/test_chain_bind.py tests/unit/gac/test_chain_bind_start_gate.py tests/unit/gac/test_error_knowledge_rule_drafts.py tests/unit/gac/test_error_knowledge_recall.py -q` | 37 passed（13 + 4 + 12 + 8，与 Q2 的分解一致） |
| 两个门同一条谓词 | 上面第二文件 | `test_start_and_closeout_agree_on_the_exempt_set` 通过 |
| 双锁口径同一份 | `python3 -m pytest tests/unit/gac/test_error_knowledge_rule_drafts.py -q` | 12 passed，含 `test_both_runtime_artifact_gates_share_one_whitelist`（AST 取两份 `WHITELIST_PREFIXES` 做集合对拍） |
| chain-bind 自检 | `python3 bin/plan/chain-bind-check.py self-check` | `PASS`，13 fixture cases |
| 全量本地门禁 | `make gac-local-gate` | `PASS (68 checks executed, ALL GREEN)`，6 项 known-unavailable skipped |
| 台账不变量 | `len(bets) == meta.total_bets` | 464 == 464，重复 id 0，本 bet 恰 1 条（`f122c9794^` = 463 且不含本 bet） |
| diff 面合规 | `git diff --name-only f122c9794^ f122c9794` vs 17 surfaces | 18 条路径全部落在声明面内；1 条已 claim 未使用（`tests/unit/gac/test_error_knowledge_recall.py`） |

### operational — PROVEN

- **live_canary**：队列在 `origin/main` 上真实可见——`git ls-tree --name-only origin/main .omo/_delivery/rule-drafts/`
  返回 3 份草案，`git check-ignore -v .omo/_delivery/rule-drafts/CR-PITFALL-GAT-004.json` 返回未忽略。
- **fresh_receipt**：本小节（上表每行都给了可原样重跑的命令）。
- **replay**：`docs/operations/workflow-waiver-2026-09-24-retro-findings.md` —— I1–I11 每条自带命令与实测输出。
- **cleanup**：本次交付未提交任何子模块指针、未改 git config、未用 `--no-verify`（三项均可由
  `git log` / `git config --get rerere.enabled` / hook 输出复测）。两个 scratch
  （`.omo/_delivery/ledger-entry-bet-y2q3-t10-202.yaml`、`.omo/_delivery/afg-rec-rule-drafts.json`）
  与 worktree `ws-rule-drafts-durability` 的释放在 run closeout 之后执行——affected-graph receipt
  必须活到 closeout，故不能在这份收据的 digest 定稿前删除。
- **复发抑制的度量**：`AGCP_REQUIREMENT_ITERATION_GATE=0` 的记录数在 2026-09-24 与 2026-09-25
  两次测量中都是 70（`.omo/_truth/governance-evidence/` 下 waiver 命名文件 136 份）。G5 之后
  治理演进 workflow 的 start 不再需要豁免；这条计数此后只能由新的交付自己往上加。

### value — NOT_PROVEN

3 份草案仍未被人审处置（`confirm` / `reject` 至今是 `NOT_IMPLEMENTED` 退 2，见发现 I3/I4），
"事故→规则"末端没有任何可测量的价值信号。`value_indicator_policy: false`，本收据不做价值背书。

### rollback

`git revert f122c9794a981d824bc0d5977afb1db7fd099477` 会同时撤回两份白名单与 3 份草案的入仓；
草案字节仍在被 revert 的历史里（`git show f122c9794:.omo/_delivery/rule-drafts/<name>.json`）。
回滚后 start 门重新变得比 closeout 更严，`python3 bin/plan/chain-bind-check.py self-check`
与 `tests/unit/gac/test_chain_bind_start_gate.py` 会立刻变红——即这条回滚不可能静默发生。
