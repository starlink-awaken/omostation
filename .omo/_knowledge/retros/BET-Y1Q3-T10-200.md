---
lifecycle: history
owner: governance-team
last_updated: 2026-09-03
title: BET-Y1Q3-T10-200 复盘
type: retro
bet: BET-Y1Q3-T10-200
date: 2026-09-03
run: 20260903T075652Z-bet-execution-24bd88be
pr: https://github.com/starlink-awaken/omostation/pull/2983
merge: 129c00389
---

# BET-Y1Q3-T10-200 复盘

> 北极星：织星是夏明星一个人的业务操作系统。它的唯一职责是：把外部进来的信号，变成他愿意署名发出去的东西，并且记住他每次改了什么。
> 指针：`docs/STRATEGY-3YEAR-PLAN-2026H2-2029.md`

## Q1 实际耗时 vs appetite？超出比例？

appetite 0.5 day；实际约 0.25 day（含 PR #2983 开出到 merge 129c00389）；超出 0%。

## Q2 done_when 是否全部通过？哪条没过，为什么？

bet 目标：给 3 个点名 docs 加 `last_updated` 并跑 batch1-6 保鲜，验证 docdate 扫描口径可用，不碰业务逻辑。

| done_when | 状态 |
|---|---|
| docs/local-compute/compute-fabric-workflow.md、docs/local-compute/omlx-cluster-architecture.md、docs/overview/cross-package-api-map.md 均有 last_updated 且格式合法 | ✅ 3 文件各 +1 行 last_updated |
| docdate 扫描对上述 3 文件不再报缺失 | ✅ doc-index 缺失数 220→217（正好 -3） |
| ledger lint 通过 | ✅ `bet-ledger.py lint` 0 新增错误 |

未过：无。

## Q3 过程中发现的与 plan 不符的事实（打假）

1. **candidate 状态缺 binding，需 spec 先行**：bet 建出来是 `candidate`，`claim` 要求 accepted_specifications 绑定。本 bet 靠 `docs/superpowers/specs/2026-09-03-docdate-freshness-batch1-spec.md`（v1.0.0 + content_digest + decision_ref）先落盘才走通 claim。
2. **ecos 子仓删文件致 COMPILER_UNAVAILABLE**：batch 范围外的 `projects/ecos` 子仓内删文件动作触发 compiler 面不可用告警，与本 bet 无关但污染了验证视野——子仓文件操作必须走子仓内 commit 三步走，不在主仓 bet 顺手做。
3. **claim scope 需 write_surfaces 预埋通配**：3 个点名文件之外，spec 与 plans 通配（`docs/notes/**`、`docs/superpowers/plans/**`、spec 明细路径、`docs/plans/3y-bet-ledger.yaml`）必须在建 bet 时一次写进 `write_surfaces`，否则执行期 claim 锁逐个补。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？

改动（PR #2983，merge 129c00389，本地 commit cd44e5a65）：

- ledger 新 bet：`docs/plans/3y-bet-ledger.yaml` +43 行（BET-Y1Q3-T10-200 条目，含 write_surfaces/accepted_specifications/verify/circuit_breaker）
- 新 spec：`docs/superpowers/specs/2026-09-03-docdate-freshness-batch1-spec.md` +51 行
- 3 docs 各 +1 行 `last_updated` 保鲜字段（compute-fabric-workflow.md、omlx-cluster-architecture.md、cross-package-api-map.md）
- 无新增 GaC 规则 / ADR / 脚本；不碰正文语义与业务代码。

验证：

- `doc-index` 缺失 220→217
- `bet-ledger.py lint` 0 新增
- CI required 全绿（PR #2983 merge）

## Q5 下一个认领本 track 的 agent 需要知道什么？

1. **先 spec 后 claim**：candidate bet 直接 claim 会被 binding 门拦，先把 spec 落到 `write_surfaces` 预埋路径并填 `accepted_specifications`（spec_ref + spec_version + content_digest + decision_ref）。
2. **write_surfaces 一次写全**：点名文件 + ledger + spec + 笔记/计划通配，执行中补 scope 就是多一轮 claim。
3. **别碰子仓文件**：ecos 等子仓删/改文件会触发 COMPILER_UNAVAILABLE 类环境噪音，batch 保鲜只动主仓 docs。
4. **batch2-6 照此模板**：每 batch = 1 bet + 1 spec + N 个 last_updated + doc-index 缺失数 -N + lint 零新增 + CI 全绿 + retro。

## Q5 补充（#2989 台账误伤修复）

`feat(scene) #2989` 的 digest 全量对齐误删/回退了多条 bet（含本 bet 条目与 T1-12 done）。
本轮在隔离 worktree 从 `762f45952`（#2989 前）外科恢复：

- 恢复 done：`T1-12` / `T10-19` / `T10-20` / `T8-02`
- 恢复缺失：`T10-200`（并钉 `delivery_accepted`）/ `T8-04`（candidate）
- 开工豁免：`AGCP_REQUIREMENT_ITERATION_GATE=0`（台账损坏导致 T1-12 capability preflight 无法 start；修复本身是 SSOT 急救）

