---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-158
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-12
type: ephemeral
---

# BET-Y1Q4-T10-158 retro — omlxc gitlink 指针收账 (probe-candidate availability fix)

## What changed

- **子仓 omostation-omlxc PR #72**（已合并）：修复 `OmlxAppAdapter.discover()`
  固定单一 `probe_model_id` 导致的 `available:false` 假阳性——单模型槽位后端
  上除被钉死模型外, 其它 chat 模型的探活几乎永远被跳过。修复后探活按
  candidate 列表逐个尝试。
- **主仓 PR #3651**（已合并, merge commit `1c128f55dea7677f7f23f5a917cc7d003c441180`）：
  root `projects/omlxc` gitlink bump 至 `e92317572eefbc539bb99c29029cf001c6261388`。

## 本 run 的职责边界

指针 bump 工程交付已由并发会话完成（#3651 合并, 全部门禁绿）。本 run 按
PITFALL-GAT-006 不重做 bump, 只做独立核验与台账收账：

- done_when①：`git ls-tree origin/main projects/omlxc` = `e92317572eef…` ✓
- done_when②：SHA 在 omostation-omlxc origin/main 可达（merge-base 判定）✓
- done_when③：#3651 合并即代表其 CI 门禁通过 ✓

## Verify 实测（2026-09-12, run 20260912T065221Z-submodule-pointer-close-70f04836）

| 命令 | 结果 |
|------|------|
| `python3 bin/gac/check-submodule-pointer-drift.py --json` | exit 0（diverged: 0, behind: 1） |
| `make governance-release-gate` | exit 0（submodule-reachability: PASS, 16 gitlinks, source=head） |
| `python3 bin/plan/bet-ledger.py lint` | OK, 404 bets, no errors |

## Rollback

若子仓 PR #72 修复引入回归：omostation-omlxc 仓库 revert 对应 commit,
再走一次 bump-pointer 事务把 root gitlink 回退到父提交。root 侧无其他
改动面, 回滚半径仅限单一 gitlink。

## Lessons

- 收账类 bet 的 D0 检查需要 worktree 内 `git submodule update --init` 才能
  解析 gitlink 对象, 否则报 "gitlink object unavailable"（非真实缺失）。
- claim 需要 affected 收据文件：`affected-graph.py --changed-projects
  workspace-root --output <file>` + `--affected-receipt <file>`（台账写面
  归属 workspace-root 项目, 不在 project-registry 内）。
