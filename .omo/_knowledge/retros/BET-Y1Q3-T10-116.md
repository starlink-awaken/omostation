---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-116
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-05
type: ephemeral
---

# BET-Y1Q3-T10-116 retro — Spine 审阅工作台 V2 与一键外发网关

## What changed

- **cockpit spine.py**（+约 160 行）：`cockpit spine review`（difflib
  opcodes 驱动的左右分栏 Diff，- / + 行标记，JSON 双输出）+ `cockpit
  spine send`（外发网关 spool 队列：临时目录完整 queued 态 → 原子 rename
  → 可插拔 sender → sent/failed 状态机 → 价值台账 fsync + os.replace
  原子追加）。
- **cockpit-ui `SpineReviewModal.tsx`**（新）：Web 审阅弹窗（分栏 Diff
  展示 + 变更计数 + 通道选择 + 确认外发按钮，data-testid 齐备）。
- **_subcommands.py**：review/send 的 argparse 注册（--draft-file/
  --edited-file/--body-file/--channel/--to/--sender/--dry-run）。
- 测试 6/6（分栏标记、JSON payload、状态机 queued→sent、dry-run 不写
  台账、failed 不写台账、参数缺省拒绝）+ 既有 spine 回归 4 个全过 +
  ruff clean + CLI 端到端冒烟（review --json / send --dry-run 入队）。

## Q3 (打假)

- 写面修正第三次发生（_subcommands.py + ledger）——argparse 注册文件
  永远不在铸造时的想象写面里。建议铸造模板默认带上。
- 外发语义的"真实送达"依赖部署侧 sender 凭证（致远 OA API / SMTP），
  本 bet 的 sent 状态在无 sender 时为 spool 确认态——retro 如实说明，
  不冒充真实外发成功。

## Q4 (遗留)

- 行级快捷微调（review 交互式编辑）未做（只读 Diff 展示 + send 闭环）；
  SpineReviewModal 的前端测试需 cockpit-ui vitest 环境，CI 兜底。
- 价值台账（value-pacing-ledger）的统计/展示面待后续 bet。
