---
title: BET-Y1Q3-T10-49 回顾
type: retro
status: planned
lifecycle: history
owner: governance-team
last_updated: 2026-08-29
created: 2026-08-29
related: []
---

# BET-Y1Q3-T10-49 retro

本文件记录 Service Gateway 对目录型 uv entrypoint 的命令编译修复。实现
完成后补齐五问、测试回执、回滚路径和 host runtime 未变更证据。

## 实施结果（2026-08-29）

- RED：目录型 `uv` entrypoint 原先生成 `uv run projects/agora ...`，新增
  回归测试按预期失败。
- GREEN：`_get_cmd` 现在对实际目录生成
  `uv run --directory projects/agora agora-mcp --sse`；文件型 entrypoint
  保持原有 `uv run <entrypoint> ...` 语义。
- 验证：`tests/test_ops_cli.py` 为 `21 passed`；dry-run 输出与 canonical
  `mcp.agora` 注册参数一致；`py_compile`、`doc-ssot-lint`、`ssot-guardian`
  和 GaC local gate 通过。
- 综合 `tests/test_ops_comprehensive.py` 仍有 19 个预存失败，集中在旧的
  status/summary/metrics/runbook 命令契约漂移；本 BET 未修改这些接口，
  不将其计入本修复回归。
- 未执行 `ops up`，未写入或加载 host plist，未启动服务进程；真正的
  `mcp.agora:7433` runtime canary 仍需独立 operational 活动。

## 正式 closeout（2026-08-29）

1. **实际耗时**：约半天，主要成本是识别 `uv` 目录入口与系统 Python/uv
   环境差异。
2. **验收**：目录型入口生成 `--directory`，文件型入口保持兼容；focused
   tests、py_compile、doc/SSOT、GaC 与 pre-push 均通过。
3. **偏差**：综合旧 ops 套件仍有 19 个历史命令面失败，集中在已漂移的
   status/summary/metrics/runbook；未在本 BET 中扩大修复。
4. **表面积**：新增 1 个 BET contract、1 个 Spec、1 个 waiver、1 个
   retro 证据文件；实现只修改既有 Service Gateway 与测试入口，没有新增
   registry、scheduler 或 runtime authority。
5. **后续**：T10-50/T10-51 分别负责 liveness budget 与 Agora 7433 环境契约；
   host service 启动仍需独立 operational 活动。
