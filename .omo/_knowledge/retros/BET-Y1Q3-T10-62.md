---
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-08-29
title: BET-Y1Q3-T10-62 OMO ingress gate recovery retro
type: retro
---

# BET-Y1Q3-T10-62 复盘

## Q1 实际耗时 vs appetite

当前约 0.5 day，符合 appetite。额外时间主要用于区分 root gitlink stale、
standalone child sibling dependency 假失败和真实 child lint failure。

## Q2 done_when 当前状态

- child allowlist seam 已修复，真实组合 workspace 的 direct-write 与 schema
  lint 均通过。
- child PR #117 的 lint、test、coverage 全部通过。
- root gitlink 尚未合入 main，故 clean recursive root CI 与 mainline 证据仍待收口，
  BET 不提前标记 done。

## Q3 与计划不符的事实

1. 17 个 direct-write findings 不是 task execution 代码绕过 broker，而是 P110
   split 后静态 allowlist 漏掉了新模块名。
2. child standalone `uv run` 的 editable 依赖指向不存在 sibling；必须在 composite
   workspace 验证，不能把环境错误当作代码回归。
3. dead imports 已在 child main 的 `18bc886` 修好，但 root pointer 仍停在旧 delivery
   revision，说明 child/mainline/root-pointer 是三个独立完成轴。

## Q4 净增减

- 净增：一个精确 allowlist 条目、一个真实模块回归测试、child CI 证据、root pointer
  更新证据。
- 净减：17 个合法 ingress 生命周期写入误报、6 个 dead-import schema gate 误报。
- 无业务行为、Documents、host schedule、runtime state、capability 或 dispatcher
  变化。
## Q5 给下一个 owner

- 等 root pointer PR 的 recursive CI；确认 `interface-check` 从 OMO lint 失败恢复。
- 合入后再回填 T10-62 completion matrix 与 child/root reachable commit。
- 随后继续 T10-59 root-oneoff registry retirement，再审 Documents 的“物理纯化完成”
  而非只看抽象下沉完成；业务价值仍保持 `NOT_PROVEN`。
