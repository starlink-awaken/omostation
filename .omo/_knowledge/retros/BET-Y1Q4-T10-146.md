---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-146
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-16
type: ephemeral
---

# BET-Y1Q4-T10-146 Retro — OMO Agent OS 成熟度差距收口 (第二次 closeout)

## 背景：为什么有"第二次"

本 BET 曾于 2026-09-15 15:56 被 closeout 为 done（commit `00eaddec97`），依据是
"8 个子 BET（T10-147~T10-165）全部 done"。但同日晚些时候 T7-07 复核
（`.omo/_knowledge/retros/BET-Y2Q1-T7-07.md`）发现子 BET T10-151（A8 外部事务
生命周期）的"已实现"证据在当时的 `projects/omo` pin 下实际不可达——是一次
假 done。该误判向上传导，父 BET T10-146 被连带回滚为 `candidate`
（`reopen_at: 2026-09-15`）。

2026-09-16：T10-151 已用真实可达的 merge commit 重新完成
（`de5cafe0662e2eaac464be69f5850df1e5bafc42`，已用 `git merge-base --is-ancestor`
核实可达）。本次是在这个教训之上，对全部 8 个子 BET 逐一重新核实证据后的
第二次 closeout——避免重复上一次"未验证即声明完成"的错误。

## 8 个子 BET 的逐一核实结果（2026-09-16 现场核实，非转引 retro 文字）

| 子 BET | 状态 | 核实方式 | 结论 |
|---|---|---|---|
| T10-147 持久化 Queue | done | `git merge-base --is-ancestor b068e61c... origin/main` | ✅ 可达 |
| T10-148 AGE-v2 SFOP 修复 | done | 现场重跑 `bin/gac/check-sfop-slots.py --json` | ✅ `ok: true` |
| T10-149 Orca R0 验证工具链 | done | 现场重跑 `bin/gac/orca-r0-verify.py --json` | ⚠️ 见下 |
| T10-150 Multica AS0 验证工具链 | done | 现场重跑 `bin/gac/multica-as0-verify.py --json` | ⚠️ 见下 |
| T10-151 A8 外部事务生命周期 | done | `git merge-base --is-ancestor de5cafe0... origin/main` | ✅ 可达（09-16 重做） |
| T10-152 A5 死引用清理 | done | `git merge-base --is-ancestor c993d8a2... origin/main` | ✅ 可达 |
| T10-153 A5 AetherForge 门面恢复 | done | `git merge-base --is-ancestor cf83e34c... origin/main` | ✅ 可达 |
| T10-165 OMO Role/Capsule/等 | done | squash-merge，`git log --grep T10-165` 命中 PR #3741/#3743/#3753 | ✅ 真实合并（squash，非直接祖先属预期） |

**T10-149 / T10-150 的诚实说明**：两个验证工具本身真实存在、可执行，
静态/配置层探针全过（T10-149: status/host_list/environment_list 3/6 PASS；
T10-150: api 30/30 PASS，schema/topology 全过）。但各有 1-3 项探针需要目标
运行时**当前正处于活跃状态**才能通过：
- T10-149 的 `run_list`/`worker_list`/`worker_show` 报错
  `"Could not read Orca runtime metadata ... Start the Orca app first"`——
  Orca app 现在没在跑（空闲态），不是代码退化。
- T10-150 的 `runtime_daemon_overlap` 报 `daemon_agents=0`——同理，现在没有
  活跃的 daemon agent 可供比对重叠。

两个工具自己的设计说明都写明"R0/AS0 = 只读准入验证，不引入 writer 控制面"——
即它们的用途是"有实际调度/准入动作发生时用来验证"，而非要求常驻应用 7x24
持续运行。本次没有为了凑"全绿"而去启动 Orca/Multica（那是更大的、超出本
BET 范围的动作），如实记录这个环境依赖，而不是隐藏或者编造证据。

## 结论

8 个子 BET 全部有真实交付（6 个有硬 merge commit 证据可直接核实，2 个工具
链本身验证通过、仅差"目标运行时当前是否活跃"这个环境态）。本 BET 自身
（`只做汇总与依赖排序，不直接产出代码`）的 `done_when` 条件
（"6 个子 BET 状态不再是 candidate"，实际已扩展到 8 个）已满足，正式 closeout。

## Scope

- `docs/plans/3y-bet-ledger.yaml`（status → done, done_at, completion_evidence）
- 本 retro
