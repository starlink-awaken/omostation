---
schema: bet-retro/v1
bet_id: BET-Y1Q4-T10-147
status: archived
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-11
type: ephemeral
completed_at: 2026-09-11
run_id: 20260911T121106Z-governance-state-mutation-cfdf2c95
implementation_run: 20260911T075418Z-bet-execution-42afae78
pr: "omostation-omo#157 omostation#3553"
---

# BET-Y1Q4-T10-147 retro — 持久化 Queue 实现

## Summary

- `PersistentQueue` 以 `LedgerBroker.append` 为唯一写路径、以
  `LedgerBroker.read(producer=...)` replay 为唯一重建路径，提供
  `enqueue` / `dequeue` / `peek` / `pending_count`，同优先级保持 FIFO。
- 初始实现与 9 项持久化/排序/异常测试由 omostation-omo#157 合并，
  child merge SHA `248ec8db61252869bfaa929eb87acff7beeeca1d`。
- 真实调用方由 omostation#3553 合并，root merge SHA
  `b068e61c198c2f7cf85fbc0f535fd92544688651`，root gitlink 指向 child
  commit `13058fe296490e1b7b12c03785fc84bf0611c274`。
- 新生产入口是
  `PolicyEnforcementService.enqueue_allowed_action(request, priority=...)`：
  先经未 mock 的 PDP admission，只有 allow 决策才把 action request 与
  decision_id 写入按 principal 隔离的 `policy-actions:<principal_id>` 队列。

## Done_when

1. **PASS** — enqueue/dequeue/peek/pending_count 均通过 ledger 事件 replay
   重建；focused suite 覆盖排序、FIFO tiebreak、重复 dequeue、异常行与队列隔离。
2. **PASS** — kill/reopen durability 测试覆盖写入、关闭 broker、重新打开后
   pending 集合保持一致。
3. **PASS** — `PolicyEnforcementService.enqueue_allowed_action` 是新的真实
   功能调用方；它没有迁移 `ecos::MessageQueue`，符合原 non-goal，同时满足
   “新建功能使用 PersistentQueue”的完成条件。

## Verification

- Focused test:
  `PYTHONPATH=../ecos/src uv run pytest -q tests/test_sovereignty_persistent_queue.py`
  → **10 passed**。
- Same gate content with machine-stable Python:
  `PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin python3 bin/gac/gac-local-gate.py`
  → **PASS, 57 checks executed, ALL GREEN**.
- omostation#3553 required checks all passed, including `phase-gate`,
  `bet-done-transition`, `gac-gate`, both `interface-check` runs, and
  `governance-verify`.
- Post-merge checks on root merge `b068e61c198c2f7cf85fbc0f535fd92544688651`
  all passed, including Governance Check, Workspace CI, Integration Tests,
  post-merge-validate, BET Done Gate, and Submodule Freshness Gatekeeper.
- Child commit `13058fe` has successful `lint`, `test`, and `test-cov`
  check runs. It has no separate child PR; root #3553 records the exact
  child SHA and provides the root admission/CI/post-merge chain.

## Operational canary

Fresh `/tmp` ledger, no mocked `decide()`:

```json
{
  "action_id": "action:t10-147-live-canary-002",
  "capability": "bos://mail/draft",
  "decision": "allow",
  "decision_id": "decision:361a369b7cd04dcaa2300886",
  "decision_path": "PolicyEnforcementService.decide (not mocked)",
  "decision_reason": "allowed",
  "item_id": "qitem:b4a20bda-ae28-4661-96e7-21123fb8c50c",
  "pending_count": 1,
  "replayed_item_id": "qitem:b4a20bda-ae28-4661-96e7-21123fb8c50c",
  "schema": "t10-147-operational-canary/v1"
}
```

The broker was closed after enqueue and reopened through
`PersistentQueue.open()`; replay returned the same item id and payload with
`pending_count=1`. A first out-of-scope episode probe correctly failed with
`policy_denied: episode_mismatch`; its temporary debug database was deleted.
All canary databases lived under `/tmp` and were removed after evidence capture.

## Q1 — Appetite

Implementation and real-caller closure completed on 2026-09-11, within the
5-day appetite. The initial implementation was already merged; this closeout
only verified the later real caller and recorded truth.

## Q2 — Done_when

3/3 passed. Engineering and operational delivery are proven. Personal value is
not inferred from queue availability and remains **NOT_PROVEN**.

## Q3 — Findings

- A BET-bound governance run generated a WorkPacket whose write scope only
  modeled the child code/test surfaces. Ledger/retro closeout therefore
  required a separate unbound governance-state-mutation run; the first bound
  run was closed blocked with no file changes.
- Local `make gac-local-gate` under `uv run --with` can make the
  `cron.ops_signal_drift_check` `python3` alias resolve to an ephemeral uv
  interpreter, violating the stable-interpreter check even with no tracked
  drift. Running the same gate entrypoint with machine-stable Python passed
  57/57. This is a local gate/environment seam, not a PersistentQueue failure.
- `bet-ledger verify --execute` could not execute the focused child test in
  the root ephemeral uv environment because pytest was absent there; the same
  command in the child project passed 10/10. D0 also needs submodule-aware
  path semantics for directory write surfaces.

## Q4 — Surface

- omostation#3553 added 67 lines and deleted 1 line across child enforcement
  code and focused tests.
- This closeout changes only the Ledger and this retro; it adds no script,
  GaC rule, ADR, or runtime state.

## Q5 — Handoff

The queue is ready for a later side-effect executor to consume admitted
actions. Such a consumer must preserve PDP admission, principal-scoped queue
ids, replay semantics, and explicit dequeue receipts; it must not treat this
engineering/operational delivery as personal value proof.
