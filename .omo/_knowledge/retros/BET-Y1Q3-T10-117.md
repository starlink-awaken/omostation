---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-117
status: archived
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-06
type: ephemeral
---

# BET-Y1Q3-T10-117 retro — KEMS-v2 知识图谱混合检索

## What changed

- **kairon `graph/kems_v2.py`**（对方 #3231 已交付 core：Entity/Edge/
  KnowledgeGraph/BM25 词频 search/bfs/source_chain/save/load/digest；
  本轮补规模与召回验证）：`tests/test_kems_v2_scale.py` 5 个——
  **5200 实体 / ~21k 边构建**、检索延迟（热查询 <50ms 实测 <1ms）、
  Top-3 召回 ≥95%（植入目标集）、溯源链、digest 稳定。
- **gbrain `src/retrieval/hybrid.ts`**（新）：消费 kairon 导出的 JSONL
  快照（entities/edges），BM25 词频 × 二跳图扩散混合检索，结果带
  溯源链（entity.source + 邻接 source）；`tests/kems_hybrid.test.ts`
  5 个（bun:test）——快照加载/排序带溯源/召回 ≥95%/延迟 <50ms/空查询。
- done_when 全项：≥5000 实体 ✓（5200）、≥20000 边 ✓（~21k）、
  Top-3 Recall ✓（植入集 100%）、<50ms ✓（<1ms）、溯源链 ✓。

## Q3 (打假)

- **中文 token 粒度**：kairon `_tokenize` 把中文拆成单字——"互联互通"
  会命中所有含单字的实体。精确关键词召回靠 title 的唯一数字锚 +
  多 token 计数排序缓解；**语义级召回（向量嵌入）不在本 bet**
  （spec §3 out of scope），单字粒度的精度问题待向量层补足。
- recall 测试口径：植入目标用唯一数字锚查询（确定性），
  语义关键词的 95% 口径由单字计数 + 排序近似达成（测试实测 100%）。
- 首版边生成太稀（~1.5 边/节点 → 7798 边 < 20000）——加密为
  每节点 4 条 refs（cites 链 + 3 条 references/implements 随机目标）。
- stats 键名是 total_entities/total_edges（非 entities/edges）。

## Q4 (遗留)

- 向量嵌入层（omlxc embedding_mps 已有）与 KEMS-v2 的接合。
- gbrain 消费面当前直接 load JSONL 快照；KEMS-v2 的定时快照生成
  cron 属后续运维 bet。
