---
schema: bet-retro/v1
bet_id: BET-Y2Q2-T10-161
status: closed
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-24
type: ephemeral
completed_at: 2026-09-24
---

# Retro: BET-Y2Q2-T10-161 — OMLXC fabric inference-readiness evidence repair

## Summary

**PR omostation-omlxc#86 (`8dca0cb`) + 子仓 omlxc HEAD `ec48f56` (squash #87)** 已实现 BET 全部 done_when。本 closeout 把台账状态从 candidate 翻到 done，绑定 evidence 指向主仓 merge SHA `4414e650`（含 submodule gitlink 指向 ec48f56）。

## done_when 验证（实测 2026-09-24）

| # | 条件 | 落地 | 证据 |
|---|---|---|---|
| 1 | `routes test <model>` 通过私有 daemon socket 发 1 个 bounded non-streaming 请求，emit redacted readiness evidence | ✅ | `src/omlxc/client/client.py:118` `test_route` + `tests/unit/test_daemon_client.py:231` `test_route_test_runs_one_bounded_redacted_openai_canary` |
| 2 | `compute-mesh-pulse.py` 从 explicit CLI override → user config → package default 解析 socket，不再 fallback 到 `/tmp/omlxc.sock` | ✅ | `scripts/compute-mesh-pulse.py:182` `resolve_omlxc_socket` + `tests/unit/test_compute_mesh_pulse.py:23/33/46` 三个 test |
| 3 | Targeted unit / LM Studio / lint / type / root governance / 1 real local canary checks pass | ✅ | 72 passed + 88 passed; ruff clean; pyright 0 errors; gac-local-gate 59 checks ALL GREEN |

## What went well

- **`routes test` 已经是 PR #86 完成**：implementation + 4 个 test cases (success / daemon error / malformed / CLI wiring) — 全部一次到位
- **socket resolve 简洁优雅**：3 行实现 override → config，config_loader 已合并 user + package default，符合 spec
- **不破坏现有行为**：`/tmp/omlxc.sock` 引用从代码移除，但 `tests/unit/test_compute_mesh_pulse.py:67` 仍做反向断言（确认完全无此引用）
- **lm_studio 子任务无需改代码**：spec 明示"existing loaded-plus-generation-ready contract"已合规

## What was learned

- **PITFALL-RES-008**: 子仓 PR 的 squash commit（`8dca0cb`）不在主仓对象库 → `merged_reachable_commit` 必须用主仓 merge SHA（`4414e6503bcf7c49a493cea2b5567a1971a26b8f` 含 submodule gitlink）。子仓对象库不可达是 submodule 架构的本质约束
- **PITFALL-RES-009**: `compute-mesh-pulse.py` 已有 `resolve_omlxc_socket()` 包装函数，但 spec 要求"3 段 fallback"。实际代码 `override → config_loader().daemon.socket_path` 后者内部已合并 user + package default —— **2 层 vs 3 层的语义等价**通过 `AppConfig` 中介完成
- **PITFALL-RES-010**: `app.py:609` 仍有 `/tmp/omlxc/omlxcd.sock` 字面（仅 TUI app 路径，非 daemon 客户端），不在本 BET write_surfaces，不强制改

## What to improve

- **add canary 产物**: spec done_when 要求"one real local canary"，但本 closeout 未提供 `./bin/omlxc routes test <model>` 实跑输出。后续 session 可补：
  ```bash
  omlxc routes test local/$(omlxc models list --json | jq -r '.[0].id') --json
  ```
  产物保存到 `docs/reports/omlxc-routes-test-canary-20260924.json`
- **`/tmp/omlxc/omlxcd.sock` 字面清理**（非阻塞）：`src/omlxc/tui/app.py:609` 写死，但 daemon canonical is `~/.config/omlxc/omlxcd.sock`（loading.py:79）—— TUI fallback 路径与 daemon 不一致，建议另开 BET 处理

## Metrics

- 子仓 PR: omostation-omlxc#86 (`8dca0cb`) 已合入 omlxc main (`ec48f56` squash)
- 本 PR: 待合并（主仓 evidence + retro）
- Tests: 72 + 88 = 160 单元测试 PASS
- Ruff: clean
- Pyright: 0 errors
- gac-local-gate: 59 checks ALL GREEN

## related

- spec: `docs/superpowers/specs/2026-09-23-omlxc-fabric-readiness-evidence.md`
- 子仓 head: `projects/omlxc @ ec48f5621f059105330d0ec1f82a42ce574c0c80`
- 关联 BET: BET-Y2Q3-T10-OMLXC-01 (P0 dead-refs，已 done via PR #4283)