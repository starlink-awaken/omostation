---
lifecycle: history
owner: governance-team
last_updated: 2026-08-29
title: BET-Y1Q3-T10-58 Automation fallout repair retro
type: retro
---

# BET-Y1Q3-T10-58 复盘

## Q1 实际耗时 vs appetite

约 0.5 day，符合 appetite。超出的部分主要花在治理流程本身：T4-02 work packet
不收 omo 路径后的 bet 铸造、spec digest 绑定、affected-graph claim 学习。

## Q2 done_when 是否全部通过

全部通过，其中两项按 bounded 语义理解：

- CLI 恢复：`bootstrap/status/compliance` 全部 ok（p74 import 修复 + omo_audit
  解环在 triage 阶段先行落地，local child branch 提交 34b0eaa7/cb9c8f59）。
- decision 防护 + 测试 7/7 过；fixture 泄漏（INBOX_DIR 未隔离）是本次定位出的
  噪音根因——163→110 个草稿是 pytest 反复运行泄漏的测试 fixture（文件名与
  fixture 数据逐一吻合），修复后不再泄漏。
- commit-msg 守卫已在本地 main 实测：chore(state) 被拒、SWARM_ESCAPE_ID 放行。
- 噪音清理：删除 13 个空 trace 占位草稿，85 个带真实 trace_id 的提案与 12 个
  DEC-* 生命周期文档保留。
- omo child branch rebase/push/pointer：**按 circuit_breaker 刻意不执行**——
  未attended extraction 会话（Blueprint）在修复期间仍活跃（15:32 仍在提交），
  强行 rebase 会踩踏共享子模块。留作 follow-up。

## Q3 与计划不符的事实

1. 噪音根因不是生产事件流，而是测试 fixture 泄漏（INBOX_DIR 未隔离）——
   原假设"resident 事件循环误发"只对了一半。
2. T4-02 的 work packet 拒绝 omo 路径 claim；bet 铸造是必须的而非可选。
3. 修复期间发现本地 main 曾被本 BET 自己的 hook 测试污染过一次指针（reset
   --soft 误操作，当即复位 origin/main）；再次验证了共享 checkout 的高危性。
4. state-sync commit→reset 循环的 reset 作者未能 100% 归因（多个 agent 会话
   共享 reflog）；commit 侧为 live agent 会话的直推行为，push 被分支保护拒绝
   是确定环节。

## Q4 净增减

- 净增：decision 空 provenance 丢弃 + 每日 dedupe 守卫、commit-msg main 守卫、
  AGENTS.md §1.3 协议条目、测试泄漏修复（INBOX_DIR 隔离）、7 个单测。
- 净减：13 个占位草稿文件；chore(state)-on-main 这类死循环提交。
- 无数据库、WAL、launchd、host 运行态修改。

## Q5 给下一个 owner

- omo child 分叉收敛（rebase 3+2 提交 onto origin/main、push、pointer
  transaction）仍未做。恢复触发条件：Blueprint 提取会话停止活动 ≥2 小时，
  或其会话明确结束。操作时先 `git -C projects/omo fetch` 再 rebase，
  全程跑 import smoke。
- resident ledger 锁竞争 15:10 复发一次（retry budget 耗尽）：工程上 WAL +
  busy_timeout≥5000ms + 重试预算均已就位，判定为极端并发突发而非代码缺陷；
  但 `-wal` 文件 4.1MB 未 checkpoint，值得在下个成熟度窗口查长读者生命周期
  （T10-48 Q5 同款遗留）。
- 机制层教训已三层固化：hook（.githooks/commit-msg）+ AGENTS.md §1.3 +
  memory（workspace-agent 记忆）。
