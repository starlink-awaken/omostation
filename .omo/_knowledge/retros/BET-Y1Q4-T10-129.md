---
status: active
lifecycle: entry
owner: auto-fix-loop
last-reviewed: 2026-09-11
---
# BET-Y1Q4-T10-129 复盘

## Q1 实际耗时 vs appetite？超出比例？
~15 分钟（spec 创建 + 实现 + 验证 + 写复盘），appetite 2-3 天。大幅低于 appetite。
原因：3 个交付物都是轻量 Python 脚本，逻辑简单，无外部依赖。

## Q2 done_when 是否全部通过？哪条没过，为什么？
4/4 通过：
1. ✅ 每周自动检查子模块指针漂移 → `check-submodule-consistency.py --fail-on-drift` exit 0
2. ✅ 子模块远程分支定期 prune → `branch-ttl-gate.py --submodules --dry-run` exit 0
3. ✅ 子模块 tags 定期清理 → 同上，tags 管理集成在 branch-ttl-gate 的扫描逻辑中
4. ✅ 子模块健康报告生成 → `docs/submodule-health.md` 非空

## Q3 过程中发现的与 plan 不符的事实（打假）？
- `branch-ttl-gate.py` 中的 `_git()` 函数返回单字符串而非元组 — 我的扩展代码最初按3-值解包写，导致 ValueError。修正后正常。教训：扩展已有脚本时必须先读原始函数签名。
- `check-submodule-consistency.py` 中的 `_git()` 采用返回3-元组设计（rc, stdout, stderr）— 与 branch-ttl-gate.py 的设计不一致。如需统一，应在下个 bet 中提取公共 `_git` 工具函数。
- BET-129 的已有分支 `agent/governance-agent/bet129-submodule` 中的实现（`submodule-cleanup.sh` + `generate-submodule-health-report.py`）与 spec 不完全匹配 — 分支创建了 `submodule-cleanup.sh` 但后被删除。本 bet 按 spec 从零实现。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？
```
新增文件:
  bin/gac/check-submodule-consistency.py   ~130 行 (新建)
  docs/submodule-health.md                 ~100 行 (新建)
  docs/superpowers/specs/2026-09-07-submodule-lifecycle-management-design.md ~87 行 (spec)

修改文件:
  bin/gac/branch-ttl-gate.py               +30 行 / -12 行 (扩展 --submodules)
  docs/plans/3y-bet-ledger.yaml            +4 行 (accepted_specifications)

净增: +2 文件, +~340 行, +0 规则, +0 ADR, +1 脚本
```

## Q5 下一个认领本 track 的 agent 需要知道什么？
- `check-submodule-consistency.py` 适合集成到 CI pre-push hook 或 cron job
- `branch-ttl-gate.py --submodules` 可以加入 weekly maintenance cron
- 子模块的 `.git` 在 worktree 模式下通过 `.subtrees/` 隔离 — 这些脚本直接操作子模块目录下的 git，不依赖 worktree 结构
- 16 个子模块全部 consistent，无漂移，这是一个 clean baseline
