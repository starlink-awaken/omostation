---
schema_version: retro/v1
lifecycle: history
type: bet-retrospective
owner: governance-team
created: 2026-08-30
last_updated: 2026-08-30
bet_id: BET-Y1Q3-T10-98
---

# BET-Y1Q3-T10-98 retrospective

## Q1. What was intended?

Restore the root governance/CI baseline needed for truthful workflow and
Documents migration closeout without weakening any check or touching Documents
content/runtime.

## Q2. What happened?

The root gate failures were separated into real causes: recursive CI checkout
could erase the root state projection, a BOS route pointed at a missing root
script, three new rules used a non-canonical M2 type, ADR-0435 was stored
outside the canonical decisions directory, and the OMO state projection had a
malformed orphan timestamp. The monthly compatibility owner was placed in
`lib/`, the Agora declaration was merged in child main, and the state broker
recomputed the task projection after a minimal syntax repair.

## Q3. What went well?

The failures were separated into root CI checkout, missing executable, M2
classification, ADR canonicalization, and child gitlink concerns before edits.

## Q4. Surface accounting

- Root: one compatibility owner moved from the `bin/` surface to `lib/`, so
  bin quota stayed net-zero; Makefile and AGENTS now point at that owner.
- Governance: three rule classifications, one canonical ADR copy/index entry,
  and one root-only CI checkout change.
- State: `system.yaml` and `tasks/registry/INDEX.md` were rewritten by the OMO
  broker; task counts stayed 290 completed / 7 planned / 1 active / 1 blocked.
- Child: one Agora BOS registry line changed and landed in child main.
- Documents and quarantine payloads: zero host mutations.

## Q5. What changes next?

The baseline is now merged and T10-70 is already closed on main. Continue with
the remaining Documents runtime owner cutovers. Do not treat this governance
repair as Documents owner parity or user-value proof.
