---
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-17
title: BET-Y1Q4-T10-168 复盘
type: retro
---
# BET-Y1Q4-T10-168 复盘

## Q1 实际耗时 vs appetite？超出比例？
本轮在隔离 worktree 中完成文档调查、导航收敛、spec registry 补登、Flight
Deck 事实边界补充和生成索引审查。实际耗时主要集中在确认主线基线、核对
近期 accepted spec 元数据和处理生成器输出；未超出既定文档收敛 appetite。

## Q2 done_when 是否全部通过？哪条没过，为什么？
文档指针、动态数量表达、近期 spec 可发现性和 Flight Deck 事实边界均已落盘。
生成型索引中，`gen-tools-index.py` 在当前 worktree 解析出 0 个 Cockpit 顶层
命令，会删除现有 111 条命令目录，因此按 circuit breaker 保留原有工具索引，
未接受该不安全生成结果。最终验证以 focused documentation checks 和本地
governance gate 为准；若工具索引生成器仍无法识别当前 CLI，应另立修复 BET。

## Q3 过程中发现的与 plan 不符的事实（打假）
`docs/superpowers/specs/README.md` 仍是手工 registry，最近交付波次新增的
accepted specs 没有自动进入导航。`SYSTEM-INDEX.md` 的 Scene/Journey 数量
已经失真，不能直接改成新的静态数量，必须改成 live-count 指针。Resident
Flight Deck 已有 Cockpit HTTP/UI surface，但当前证据不足以宣称
`bos://resident/*` 或 `omo resident` CLI 已完成接线。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？（贴 surface 输出）
新增 1 个文档 retro 和 1 个 accepted spec；更新 SYSTEM-INDEX、Resident
architecture、spec registry，以及 knowledge/agent/project generated indexes。
未新增代码、GaC 规则、ADR、脚本或子模块指针。由于工具索引生成器输出会造成
无依据的大面积删除，`docs/INDEX-TOOLS.md` 保留原有命令表，问题转为后续
generator/parser hardening 任务。

## Q5 下一个认领本 track 的 agent 需要知道什么？
先修复 `bin/ssot/gen-tools-index.py` 对当前 Cockpit CLI 注册方式的解析，
并在有完整输入的 worktree 中重新生成 `docs/INDEX-TOOLS.md`；修复前不要接受
`cockpit_cmds=0` 的结果。spec registry 适合先维持 surgical recent-wave
登记，长期再迁移为可审查的生成型索引。T10-151 仍以权威复核和 accepted
spec 为事实来源，状态不得回写为已完成。

## Addendum — 2026-09-17 follow-up session (run 20260917T023819Z, T10-168 closeout)

- Triaged stale worktree `ws-t10-168-doc-convergence` (uncommitted README
  opening-wave section, read-only, not modified): all 12 rows verified
  against on-disk frontmatter (`status`/`bet_id`/`last-reviewed` exact
  match) and adopted verbatim into `docs/superpowers/specs/README.md`.
- Adopted prior unmerged commit `76c2049e81`'s single row for
  `2026-09-17-t7-02-assisted-escalation-design.md` (accepted,
  BET-Y3H1-T7-02); re-verified frontmatter before registering.
- Added missing `tracks: T10-DOCS` registry entry in
  `docs/plans/3y-bet-ledger.yaml` (bet entry itself was already on main).
- Known remainder (follow-up, not this BET): ~95 accepted specs dated
  2026-09-02—09-09 remain unregistered in the manual registry. Prior
  sessions scoped "recent" as the 09-10+ burst; backfilling the full
  09-02—09-09 range by hand repeats the cost the spec non-goal warns
  about — route to the generated-index migration instead.
