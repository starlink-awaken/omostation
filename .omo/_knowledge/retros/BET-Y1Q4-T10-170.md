---
schema: md/v1
status: active
lifecycle: history
owner: auto-fix-loop
last-reviewed: 2026-09-25
type: ephemeral
---

# BET-Y1Q4-T10-170 复盘

## Q1 实际耗时 vs appetite？超出比例？
约 2 小时 vs 4 hours appetite，未超。

## Q2 done_when 是否全部通过？哪条没过，为什么？
不适用：本次为 explicit user waiver 授权直接修复 agora，非 BET 原定 write_surfaces。

## Q3 过程中发现的与 plan 不符的事实（打假）？
- `projects/agora` 的 PEP provider 两个软依赖模块（`ecos.ssot.tools.policy_provider`、`omo.integrations.pep_provider`）当前均不存在，degraded bypass 是唯一可行路径，但监控不足。
- `pyproject.toml` 缺少 `omo` / `metaos` 依赖，导致 PEP 桥接类无法加载。
- `agora-services.json` 硬编码端口/endpoint，无环境变量覆盖入口。
- `BackendHealthChecker` 的 `heartbeat_report` 不含 transport 类型，dashboard 无法区分 stdio standby 与真 dead。

## Q4 净增减：代码行 / 文件 / GaC 规则 / ADR / 脚本？
- 代码行：+412
- 文件：`pyproject.toml`、`policy_enforcement.py`、`registry.py`、`health.py`、`bin/agora/check-submodule-pins.py`
- 规则：0
- ADR：0
- 脚本：+1 (`check-submodule-pins.py`)

## Q5 下一个认领本 track 的 agent 需要知道什么？
- `_PEPAdapter` 已桥接 `omo.resident.pdp_pep:PEP` 到 Agora PEP SPI，后续无需重复 degraded bypass。
- `AGORA_SVC_<NAME>_ENDPOINT` / `AGORA_SVC_<NAME>_PORT` 已支持覆盖静态配置。
- 子模块 pin 检查脚本可扩展更多规则。
