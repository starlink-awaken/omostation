---
schema_version: retrospective/v1
type: retro
title: BET-Y1Q3-T10-119 Closeout Retro — 双机雷雳 5 算力织网双向备份与断网容灾自愈
bet_id: BET-Y1Q3-T10-119
status: closed
owner: xiamingxing
created: 2026-09-03
last-reviewed: 2026-09-03
---

# BET-Y1Q3-T10-119 Closeout Retro

> **TL;DR**: 在 `dma_daemon.py` (ADR-0437) 物理层基础上, 新增 `failover.py` 高层故障转移控制器. 12/12 tests PASS (7 unit + 5 integration). 实测: 3次连续心跳失败 → HEARTBEAT_LOSS, 6次 → DEGRADED, 重连后 → DUAL_RECOVERED.

## Deliverables

- `projects/omlxc/src/omlxc/dataplane/failover.py` (315 lines): `FailoverController` + `HeartbeatSnapshot` + `from_dma_daemon_telemetry()`
- `tests/test_failover_controller.py` (7 unit tests)
- `projects/omlxc/tests/integration/test_dual_node_failover.py` (5 integration tests)
- `docs/superpowers/specs/2026-09-03-t10-119-dual-node-failover-design.md` (spec, sha256:2e975a34...)

## 4 lessons

1. **dataclass + importlib 在 py3.14 的 bug**: 用 `importlib.util.spec_from_file_location` 加载含 `@dataclass` 的模块会触发 `cls.__module__` 为 None. 解决: `exec(compile(source, path, "exec"), mod.__dict__)` 绕过 spec.
2. **win-take-all 在单进程测试有限制**: 两个独立 controller 各自有 lock, 都会 grant takeover. 真实部署需用 file lock 共享. 测试中用 audit log 验证两个 event 都记录来表达.
3. **size-based rotation 必须先 read 再 write**: 1MB 阈值检测后, rename 旧文件 .jsonl.1 → .jsonl.10, 移出当前 .jsonl 才能重新 append. 否则下次 fcntl 锁住会死锁.
4. **pytest rootdir 自动推断**: 从 `projects/omlxc/tests/integration/` 跑测试, rootdir 自动设为 `projects/omlxc/`. `parents[N]` 计算要小心.

## Test results

```
$ pytest tests/test_failover_controller.py -v
============================== 7 passed in 0.22s ==============================

$ pytest projects/omlxc/tests/integration/test_dual_node_failover.py -v
============================== 5 passed in 0.20s ==============================
```

## 已知限制

- **takeover race 在单进程测试下是 local-impl artifact**: 两个 controller 各自 grant. 真部署需 file lock.
- **reconnect 计时用 local clock**: 不模拟真实 3 秒, 只验证 state transition + 3s contract (instant in test).

## Action items

- cockpit submodule PR: 添加 north_star + failover cockpit 入口
- 后续 v2 候选: file lock 共享 takeover, multi-region failover
