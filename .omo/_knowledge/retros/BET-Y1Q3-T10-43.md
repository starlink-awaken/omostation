---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-43
lifecycle: history
owner: governance-team
last_updated: 2026-08-28
title: BET-Y1Q3-T10-43 implementation retro
type: retro
---

# BET-Y1Q3-T10-43 implementation retro

## Outcome

Five OMO governor/verifier/replay/alert/status records were registered as
lifecycle-only launchd observations but had no machine plist contract. They are
now explicitly `generate: false`, so the plist generator neither writes nor
compares a fictitious host service for them.

The generator now validates every generated launchd declaration before rendering
it. Missing label, interpreter, or entrypoint is a deterministic validation
violation rather than an unhandled `KeyError` in `--check`.

## Evidence

- RED: focused validator tests failed because `validate_service_declaration`
  did not exist.
- GREEN: `tests/test_gen_service_configs.py` passes with lifecycle-only and
  missing generated-launchd-field coverage.
- `--validate --json` passes for the repaired registry.
- `--check --json` reports existing host plist drifts structurally; it does not
  write plist files or raise `KeyError`.
- CI without a launchd observer directory now returns a structured skip instead
  of treating every absent plist as drift; declaration validation remains
  blocking.

## Residual

Reported host drifts are outside this BET. They require a separate host-config
reconciliation with explicit plist ownership and rollback evidence.

## 2026-08-28 增补 (claim 复核)

P73 D1 反向陷阱实锤: claim 时按 BET 文本预期"五条待修 + KeyError 待修", 实况
#2405 已完成主体。本轮复核: `--validate --json` exit 0 · `--check --json`
无 KeyError · 既有 3 测试通过。

补 spec Verification 3 后半句盲区: 新增
`test_observer_unavailable_does_not_mask_malformed_declarations` — observer
缺失 + 缺 label 声明 → skipped report `ok:false` + exit 1 (skip 不是逃生门)。
4/4 passed。

关账路径: merge 后 engineering → VERIFIED (merged_reachable_commit), 人工
operational/value receipt → `bet-ledger.py complete BET-Y1Q3-T10-43`。
