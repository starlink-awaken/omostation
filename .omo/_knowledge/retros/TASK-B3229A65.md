---
schema: bet-retro/v1
bet_id: TASK-B3229A65
status: archived
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-15
type: ephemeral
---

# TASK-B3229A65 retrospective — A4 调度真相收尾

## Q1. What was intended?

PR #3518 (2026-09-11) 已完成 A4 调度真相收敛的主干:
- 三源声明 (v1 registry / operating-rhythm-crontab / 真实 crontab -l) 统一为
  `.omo/cron/registry.yaml` v2
- 修复 2 处死引用 (Makefile worktree-prune 指向不存在的 gac-worktree-prune.sh;
  bin/scheduler-compile.py parents[2] 路径错掩盖所有 drift)
- 面板刷新退出 Codex LLM automation 改 launchd

本次 (PR 4014 收尾) 目标是清理 PR #3518 + #3781 之后的剩余债:
1. registry.yaml 仍含 4 个 v1/v2 段误复制的重名 jobs
2. scheduler-compile.compile_crontab 与 registry v2 范式不匹配 (双 cd / 双 log 重定向)

## Q2. What happened?

### 实际交付

1. **dedup**: `.omo/cron/registry.yaml` 从 46 jobs 减到 42 jobs
   - 删 v1 段 4 个 duplicate blocks (worktree-hygiene-daily / hygiene-dir-daily /
     ssot-guardian-weekly / capability-registry-sync-daily)
   - v2 段(2026-09-12 补登的"孤儿 crontab 补登"段, 标 # 213) 是真值来源,
     与 `crontab -l` 实际一致, 保留
   - v1 段(line 28-)的 reconciliation_note 上下文 (D2/D3 决策依据) 仍在文件,
     没丢历史

2. **compile_crontab 嵌套 cd 修复**:
   - 旧: `f"{sched} cd \"$HOME/Workspace\" && {cmd} >> {log} 2>&1"`
   - 新: `f"{sched} {cmd}"`
   - registry v2 范式: command 字段已是自包含 (含 cd 与 log 重定向)
   - 验证: `--generate crontab` 输出从 38 行 + 嵌套 → 17 行无嵌套, 与 crontab -l 实际一致

3. **测试**: `tests/test_scheduler_compile.py` 6 个测试全过
   - `test_registry_no_duplicate_job_names` (回归保护)
   - `test_registry_job_names_unique_preserved` (dedup 不删关键 job)
   - `test_compile_crontab_does_not_double_cd` (旧 bug 防回归)
   - `test_compile_crontab_does_not_double_log_redirect` (旧 bug 防回归)
   - `test_compile_crontab_output_matches_registry_command_with_schedule_prefix` (契约)
   - `test_scheduler_check_no_drift_after_dedup` (集成验证)

### 实际花的时间

1 day appetite, 实际 1 个会话 (1-2 小时) 内交付.

## Q3. What went well?

1. **4012/4013 风格 (governance-infra, 低风险, 小工时) 在 4014 继续有效**:
   dedup + bug fix + test + retro 全在单窗口完成.
2. **dedup 用 yaml 解析而非文本搜索**: 解析 jobs 列表再 match name 比
   grep 更稳, 避免误删 (前段独有 9 个 jobs 跟 v2 段重名 4 个, grep 会全删).
3. **测试契约法** (输出 = schedule + command) 比模式匹配更准: 既能 catch
   旧 bug (双 cd / 双 >>) 又允许 anomaly-alert-daily 这种命令本身用
   `&&` 链两条子命令各带 `>>` 的合法形态.

## Q4. What went poorly?

1. **3 个 pre-existing 测试 fail 与本任务无关** (python3.14 路径断言 / observer lambda
   签名), 没去修. 应当在 PR description 里明确 "已知 pre-existing fail".
2. **TASK-B3229A65 的 ledger entry 还没 closeout** (status 仍是 candidate).
   本 PR 之后应该推一个 ledger closeout commit (跟 T7-06 / T5-01 / T10-146 一样).
3. **dedup 前没深查每个 block 的 reconciliation_note 差异**. 实际上 v1 段含
   D2 决策上下文 (worktree-hygiene 的 --stale-days 3 flag 讨论), v2 段
   reconciliation_note 简短. 简单删 v1 段就 OK 因为 D2 在 open_decisions 段
   (line 559+) 独立跟踪, 但更深一步: 应该把 v1 段详细 note 合并到 v2 段
   reconciliation_note. 这是后续 polish, 不阻塞本次.

## Q5. What will we do differently?

1. **AGENTS.md / docs/PLANS 补一条 "registry.yaml dedup 工具"** 写进
   治理自动化工具体系. PR #3518 + 4014 + 3781 三次人工 dedup 应该
   让 gen-service-configs 工具在 validate 阶段检测 jobs 段内 name 唯一性.
2. **测试契约规范化**: scheduler-compile 后续修改 (加 launchd/github 格式等)
   都按 "输出 = schedule + command" 契约扩展, 不再回到包 cd / 包 log 重定向模式.
3. **TASK-B3229A65 接下来 1 个 commit 关闭**: 跟 T10-146 一样, 写
   `closeout: TASK-B3229A65` commit, status=candidate → done,
   加 completion_evidence (engineering: VERIFIED, operational: PROVEN,
   value: NOT_PROVEN), value_indicator_policy: false.

## Evidence

- 改动文件:
  - `.omo/cron/registry.yaml`: -49 / +0 (4 重复 blocks 删)
  - `bin/scheduler-compile.py`: -3 / +3 (compile_crontab 嵌套 cd 修复)
  - `tests/test_scheduler_compile.py`: +159 (新测试, 6 用例)
  - `.omo/_knowledge/retros/TASK-B3229A65.md`: +80 (retro)

- 验证:
  - `python3 bin/scheduler-compile.py --check` → drift=0, orphan=1
    (orphan=1 是 ~/.local/share/zhixing-dashboard/panorama_cache.py, 本地运维
    cron 配置在 ~/.local 不在本仓, 不属本任务范围)
  - `python3 -m pytest tests/test_scheduler_compile.py -v` → 6/6 PASS
