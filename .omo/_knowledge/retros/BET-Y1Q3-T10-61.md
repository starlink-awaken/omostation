---
lifecycle: history
owner: governance-team
last_updated: 2026-08-29
title: BET-Y1Q3-T10-61 meta-doctor refs-only registry binding retro
type: retro
---

# BET-Y1Q3-T10-61 复盘

## Q1 实际耗时 vs appetite

截至本次交付约 0.25 day，符合 appetite。主要额外耗时来自 affected-graph
claim 前置和对本机环境失败的分层核验。

## Q2 done_when 当前状态

- Registry entry 已补齐 `--refs-only`，通过 targeted binding test。
- `check-ci-surfaces` 无 error，说明注册表结构和接线没有新增漂移。
- Clean CI / full local gate 尚未获得通过证据，因此 BET 保持 candidate，不能
  宣称 delivery accepted。

## Q3 与计划不符的事实

1. 入口 workflow 已经传递 `--refs-only`，真正缺口只在 registry-driven mirror，
   不是 runner 参数管道缺失。
2. 本机 full runner 会读取真实 LaunchAgent，暴露两个与本 BET 无关的 dead refs；
   不能通过删除或忽略引用来“修复”本次参数契约。
3. 当前主线仍带有重复的 T10-58 registry/ledger 状态和脏子模块指针；这些应由
   T10-59/后续独立 slice 收敛，不能混入 T10-61。

## Q4 净增减

- 净增：一条 canonical registry 参数、一个精确回归测试、一个 spec/report/retro
  证据链。
- 净减：registry runner 与 governance-check job 之间的参数语义漂移。
- 无 Documents 内容、host schedule、runtime state、meta-doctor 规则或 dispatcher
  改动。

## Q5 给下一个 owner

- 先以 clean recursive CI checkout 验证 governance-check interface job；若通过，
  回填 T10-61 completion evidence。
- 随后在独立分支推进 T10-60 capability split，并把 T10-59 的 root-oneoff
  retirement rebase 到最新 main；先处理重复 T10-58 再宣称 Documents 注册表闭环。
- 保持 `value: NOT_PROVEN`，本修复只证明治理接线，不证明 Documents 业务价值。
