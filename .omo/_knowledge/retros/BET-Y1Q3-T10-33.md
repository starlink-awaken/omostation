# BET-Y1Q3-T10-33 retro

The legacy signal rotation mixed machine-signal normalization with a write to
the human-owned Documents signal ledger. The new preflight makes the boundary
explicit: it classifies the ledger and reports machine-signal findings without
rewriting Documents.

The live source currently contains three human/user signals and no machine
signals, so the preflight is a truthful exit-0 baseline. The Monday 06:10 line
was replaced under run `20260827T175111Z-governance-state-mutation-702337a3`
using accepted-20260831; the old line is zero, the new line is one, and 108
unrelated lines are byte-identical. The legacy writer remains available for
rollback/parity but is no longer scheduled.
