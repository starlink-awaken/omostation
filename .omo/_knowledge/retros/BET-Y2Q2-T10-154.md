---
schema_version: retro/v1
title: BET-Y2Q2-T10-154 retro — A9 strategy retro full-scan repair
bet_id: BET-Y2Q2-T10-154
status: completed
lifecycle: history
owner: governance-team
type: retro
last-reviewed: '2026-09-22'
---

# BET-Y2Q2-T10-154 · A9 strategy retro full-scan repair

## Q1 · 预定意图

按 spec `docs/superpowers/specs/2026-09-22-a9-strategy-retro-full-scan-design.md`：
把 `~/.local/share/zhixing-dashboard/strategy_sources.py` 的
`MAX_RETRO_FILES` 从 500 提到 1000，使 502+ 条 retro 全量读取，
消除 `retros:sample_budget` 截断；补一条 collector 测试断言
无截断且状态 OBSERVED；让 cockpit `source_states` 中 strategy 变 OK。

## Q2 · 实际发生

- 认领后发现实现**已预先存在于 home 侧**：
  `strategy_sources.py:34` 已是 `MAX_RETRO_FILES = 1000`
  （mtime 2026-09-22 09:52 本地，早于本 run 启动），
  `test_strategy_sources.py:128` 已有
  `test_retro_full_scan_does_not_report_sample_budget_at_current_limit`。
  仓库侧无任何 PR 改动过这两个 home 文件。
- 本 run 实际做的是验收与归档：
  1. `python3 test_strategy_sources.py` → Ran 11 tests, OK（exit 0）。
  2. 实跑 `collect_strategy_sources()` →
     `hard_gaps: []`，`counts: {truncated: 0, runs: 52, retros: 506}`，
     输出零 `retros:sample_budget`。
  3. 刷新后 `current.json` →
     `source_states.strategy.status = "OK"`
     （last_success_at 2026-09-22T02:47:52Z）。
  4. `bin/gac/gate-evidence-receipt.py --gate A9` →
     receipt 生成于 `.omo/_delivery/environment-evidence/a9-20260922.yaml`
     （该目录被 .gitignore 忽略），归档复制到
     `docs/reports/a9-20260922-gate-receipt.yaml`（内容字节一致），
     verdict **PARTIAL**（声明态，exit criteria 全 not_evaluated）。
- spec 四条 acceptance 全部满足；ledger 变更 = status done +
  completion_evidence matrix + 本 retro + receipt 归档。

## Q3 · 偏差与打假

1. **实现先于认领**：代码修复在 claim 前已在 home 侧完成，
   本 bet 没有"写代码"的交付 PR。如实记录，不伪造提交来源；
   engineering 的 merged_reachable_commit 用 spec 绑定提交
   `7758c80b4`（PR #4169，origin/main 可达），而非虚构的代码 PR。
2. **A9 仍 PARTIAL，不提升**：done_when 第 3 条"A9 gate passes"
   超出本 bet 范围——receipt 明言
   `unlock_conditions: T10-166 ASD 面板契约 + 驾驶舱(T10-163)呈现；PARTIAL≠PASS`。
   本 bet 只修复 strategy 数据源新鲜度；按 spec non-goal
   不把部分证据报成 PASS，A9 全绿由 T10-166/T10-163 承接。
3. **D0 铁律**：write_surfaces 含两个精确 home 路径
   （`~/.local/share/zhixing-dashboard/strategy_sources.py`、
   `test_strategy_sources.py`），`git ls-files` 永远不可见，
   `bet-ledger.py complete` 需 `--force`，理由即此结构性限制
   （glob 形式本可豁免，精确路径不行）。

## Q4 · 教训

- 领 bet 前先探测目标现状（PITFALL-GAT-006 的 home 侧变体）：
  本例若直接"再改一遍"会制造无谓 diff。
- "验收 + 归档"型 closeout 的 evidence 应指向真实存在的
  receipt/commit，宁可用 spec 绑定提交也不虚构交付提交。
- gate 声明态 PARTIAL 与 bet 完成是两个正交状态，
  retro 里分开表述，防止下游把 bet done 误读成 gate green。

## Rollback

`MAX_RETRO_FILES` 恢复 500；collector 继续以精确截断原因 fail-closed。
