---
bet_id: BET-Y2Q2-T10-154
created: '2026-09-22'
type: retrospective
---

# BET-Y2Q2-T10-154 复盘

## Q1 Actual time versus appetite

Implementation and focused verification completed in less than one hour,
within the four-hour appetite.

## Q2 done_when 是否全部通过？

- PASS — `MAX_RETRO_FILES` now covers the current 506-file retro corpus; the
  refreshed strategy collector reported 506 discovered / 506 read / 0 truncated.
- PASS — the regression test proves 501 retros scan without
  `retros:sample_budget`; focused collector tests are 11/11.
- PASS — fresh cockpit strategy source state is `OK` and the direct collector
  state is `OBSERVED` with zero hard gaps.
- FAIL — the full A9 gate remains PARTIAL, not PASS. The fresh check reported
  `dashboard_live` and `cockpit_sources`; the latter is caused by dashboard-local
  `workflow` and `scheduler` collectors trying to execute `bin/agent-workflow.py`
  and `bin/scheduler-compile.py` under the dashboard repository. Those paths are
  outside this BET's authorized write surfaces, so the BET must remain blocked
  instead of promoting the strategy-only improvement to a false green.

The dashboard implementation is immutable at dashboard commit
`c9540b3c91afa709c801c6537a617369a9df11f5`, tagged
`bet/BET-Y2Q2-T10-154-20260922T015414Z`. The handoff note's `e487415...` SHA was
a later unrelated dashboard UI commit and was not used as completion evidence.

## Q3 过程中发现的与 plan 不符的事实（打假）

- The initial regression assertion expected the isolated fixture to be fully
  `OBSERVED`, but unrelated optional library gaps made it `PARTIAL`. The test was
  narrowed to the exact retro-budget contract while preserving fail-closed
  reporting for unrelated gaps.
- The prior handoff identified Orca/Multica as possible remaining A9 failures;
  fresh evidence shows both are currently `OK`. The actual remaining bad cockpit
  sources are only `workflow` and `scheduler`, plus the stale dashboard snapshot.
- The handoff's implementation SHA `e487415...` was inaccurate and pointed to an
  unrelated later UI commit; `c9540b3...` is the verified implementation SHA.

## Q4 净增减

Dashboard-only: one constant increased and one regression test added. No root
rules, ADRs, scripts, or implementation files changed. Surface output is run
separately and does not count dashboard-only artifacts as root surface growth.

## Q5 下一个认领本 track 的 agent 需要知道什么？

Open a separate governed transaction for the A9 runtime projection path bugs:
workflow and scheduler collectors must stop assuming a dashboard-local `bin/`
checkout. Then refresh the dashboard projection and recompute the full A9 gate.
Do not fake source states, promote this BET to done, or treat PARTIAL as PASS.
