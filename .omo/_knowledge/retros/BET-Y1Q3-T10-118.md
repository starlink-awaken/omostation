---
schema: bet-retro/v1
bet_id: BET-Y1Q3-T10-118
status: active
lifecycle: history
owner: governance-team
last-reviewed: 2026-09-06
type: ephemeral
---

# BET-Y1Q3-T10-118 retro — 三域 LoRA 蒸馏与动态热插拔

## What changed（工程交付）

- **`omlxc/dataplane/lora_manager.py`**（新）：`partition_buffer_by_domain`
  （replay buffer 按域关键词分片，email/tech 特异性优先于 gov 宽词）、
  `LoraAdapterManager`（注册表持久化 + `activate/deactivate` 热插拔 +
  `adapter_for_domain` 路由查询）、`distill_all`（逐域复用 T10-105
  `dispatch_distill` 三级诚实派发，域分片写临时 buffer）。
- **cockpit spine**：`spine lora --list-adapters [--eval]` 注册表面 +
  `spine draft --domain X` 自动激活对应适配层（存在才挂载）。
- 测试 6/6（分片/空 buffer/注册表热插拔往返/pending 诚实/真实 dispatch
  mock/注册表形态）+ 既有 spine 回归 12 个全过，ruff clean。

## Q3 (打假)

- **真实 32 条 buffer 分片冒烟：gov 32 / tech 0 / email 0**——现有样本
  全为公文政务类，tech/email 域样本不足。done_when[0] 的三个权重包
  **只能诚实产出 pending 记录**（gov 域样本量已够但本机无 MLX 后端，
  dispatch 报 needs_mlx；不以合成权重冒充）。
- done_when[2] 的对齐度提升 ≥30% 需要真实训练产出 adapter 后才能
  测量——评估框架（ROUGE-L，T10-105 交付）已就位。
- 分片教训：gov 的单字"函"会吃掉 email 的"函复"——关键词分片的
  特异性排序（email/tech 先于 gov 宽词）是正确性前提。
- 写面修正第 6 次（测试/parser/ledger 缺失）——铸造模板问题再次确认。

## Q4 (遗留 → 收账门)

- 三个权重包：gov 待 MLX 后端安装（或 M4 节点路由），tech/email 待
  对应域真实样本积累（≥8 条/域）。**bet 维持 candidate 不收账**。
- 对齐度 ≥30% 的评估需 adapter 真训后跑 evaluate_alignment。
