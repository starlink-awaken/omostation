# BET-Y1Q4-T10-145 复盘（Wave A → D non-terminal）

> BET overall remains **candidate** / overall **evaluating**.  
> operational/value remain **NOT_PROVEN**.  
> `value_indicator_policy=false`.  
> 本文件是 Wave D 证据面之一；**不是**毕业完成声明。

## Q1 实际耗时 vs appetite？

同日连续交付 Wave A → B1–B4 → C → Spec 1.7.0 binding → Wave D 非终态报告。  
Appetite（12 天含强制 24h 观察）未耗尽；**24h 观察钟尚未启动**（缺 Task 16 host 激活收据）。

## Q2 done_when？

已完成：仓库分区 A/B1–B4/C + 对应 non-union binding 至 1.7.0。  
未完成：host R0 激活、24h 连续观察、三生命周期实操、独立审计、毕业/价值判定、WP2。

## Q3 打假发现

1. Binding 必须 unbound `governance-state-mutation`；挂 bet WorkPacket 会 SCOPE_MISMATCH。  
2. Squash 后 ledger blob 可能因并发 PR 不等；以 T10-145 内容等价为准。  
3. zsh `for path in` 会覆盖 `PATH`。  
4. Wave C reachability 全量 `--require-main` 需完整 submodule init；交付核验用 `--changed-from`。  
5. `test_agent_workflow` 若干红为 aetherforge 未 init 环境噪声，与 omo pin 无关。  
6. 观测站 `#gov` A4：crontab 有 3 条已安装但未入 registry 的 orphan（harness-cron / cli-availability-probe / ops check-signals）。  
7. 未满 24h、未激活 host 前写 `done`/`done_at` 属违规。

## Q4 净增减（仓库）

| 波次 | PR | 要点 |
|---|---|---|
| A | omo#154 | child claims broker |
| B1 | #3535 | root adapter/fence |
| B2 | #3554 | 本地入口 proposal-only |
| B3 | #3564 | API/shim fail-closed |
| B4 | #3575 | 云端 workflow proposal-only |
| C | #3581 | omo gitlink → `83c27519b` |
| 1.7.0 | #3583 | Wave D evidence binding |
| D | （本 PR） | 非终态 shadow graduation JSON + retro |

## Q5 下一 agent

1. **不要**把本报告当毕业。  
2. 若要开观察钟：先取得独立 host 授权并完成 Task 16 激活 CAS，再启 foreground 24h observer。  
3. 并行可治理项：登记 A4 三条 crontab orphan 到 `.omo/cron/registry.yaml`（安装态对齐登记源）。  
4. operational/value 仍 NOT_PROVEN；禁止把 CI/工程绿计入个人价值。
