---
status: active
lifecycle: entry
owner: governance-team
last-reviewed: 2026-08-30
type: ephemeral
status: archived
---

# BET-Y1Q3-T10-102 retro

## Q1. What changed?

The consumer audit now tokenizes shell paths independently from argv, preserves
quoted spaces, shares one execution-candidate predicate, detects extensionless
control executors, and returns nonzero when forbidden Documents executors are
present.

## Q2. What is proven?

Ten focused tests, Ruff, and Python 3.9 parsing pass. A fresh host audit turns
the previous false green into one exact forbidden Scheduled executor with zero
unmatched execution surfaces. Historical T10-23/T10-36 test receipts are
re-attested because this BET intentionally changes their shared regression
file; their completion verdicts are not promoted or otherwise changed.

The implementation is reachable from root main at
`51b4f4c92e3dab225717e923a8deb0b0d9961772`; required PR checks and the
post-merge live replay pass.

## Q3. What remains?

`vault-daily-health` still calls the quarantined Documents shell. This is now a
truthful host cutover requirement, not part of the detector implementation.
The volatile ZCode client-state directory also continues to prevent a stable
full-tree L4 audit.

## Q4. What is the rollback boundary?

Revert the parser/classification patch and tests. No host or Documents state was
changed, so no external rollback is required for T10-102.

## Q5. What value is proven?

Engineering and operational detection are proven. Principal-bound value is
`NOT_PROVEN`; this BET only restores the honesty of a governance gate.
