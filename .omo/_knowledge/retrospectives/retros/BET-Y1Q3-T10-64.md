---
lifecycle: history
owner: governance-team
last_updated: 2026-08-29
title: BET-Y1Q3-T10-64 Cockpit decide write-boundary recovery retro
type: retro
---

# BET-Y1Q3-T10-64 复盘

## Q1 实际耗时 vs appetite

当前约 0.5 day，符合 appetite。中途通过 child main 历史提交核对，发现
atomic-helper 修复仍会被 root gatekeeper 识别为 caller-side direct helper，
因此收敛到 canonical scenario-inbox adapter。

## Q2 done_when 当前状态

- decide 的五个公开动作仍保留，并通过 canonical scenario-inbox helpers
  读写。
- 新增 canonical adapter test、child PR CI 和 root 定向 gate 均通过。
- root gitlink 已准备指向 child main merge commit；root PR 合入与最终 required
  CI 仍待完成。

## Q3 与计划不符的事实

1. child main 已有 `2d44b650` 的 atomic-helper 中间修复，但它不能清除
   gatekeeper 对 caller-side mutation helper 的检查。
2. 只有将 persistence owner 收敛到已有 scenario-inbox engine，才同时满足
   “无 caller direct write”和“无第二存储面”。
3. 完整 Cockpit suite 的 25 个 personal-episode authority failures 是既有基线，
   没有修改其测试或实现来掩盖它们。

## Q4 净增减

- 净增：canonical decide adapter、默认 scene/journey 兼容创建、状态适配测试。
- 净减：legacy `.omo/state/decision-inbox.json` writer 和 caller-side direct I/O
  gate failure。
- 无新存储、dispatcher、broker、schema、capability、Documents 或 host/runtime
  面变化。

## Q5 给下一个 owner

- 合入 root pointer PR 后重新核对 child-main reachability 与 direct-omo-io gate。
- 后续再评估是否需要清理 legacy decision JSON 历史存量；那是数据迁移问题，
  不应混入本次兼容入口修复。
- 继续保持 `value: NOT_PROVEN`，等真实主人使用与署名闭环证据。
