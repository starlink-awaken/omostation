---
title: BET-Y1Q3-T10-31 回顾
type: retro
lifecycle: history
owner: laowang-agent
last_updated: 2026-08-28
created: 2026-08-28
related: []
---
# BET-Y1Q3-T10-31 retro

Bridge refresh is a write path into Documents generated views, not a content
authority. First replace scheduled execution with a read-only Workspace preflight
and preserve the legacy writer for rollback/parity. Cockpit must later own the
user-facing projection before the writer is permanently retired.

The candidate uses clean `accepted-20260908`. The 06:05 replacement completed under
run `20260827T164501Z-governance-state-mutation-1d0e88e3` after a full crontab
backup. The old line count is zero, the new line count is one, and 108
unrelated lines are byte-identical. The post-cutover preflight is `ready` with
exit 0: all three Workspace sources and both dashboard markers are present,
while the preflight remains read-only.

Operational lesson: on this macOS host, `/usr/bin/crontab` rejected the deep
evidence path even though the file existed. Installing from a checked temporary
file in `/tmp` succeeded; the failed attempts made no crontab change and the
before SHA remained unchanged until the successful install.

The current 06:05 owner line is on `accepted-20260908` after T10-46 release
root convergence. A fresh bridge preflight returned `documents.bridge-preflight.v1`
with status `ready`, exit 0, sources_ready=3, markers_ready=2, and errors=[];
focused tests pass 4/4. No legacy writer process was observed. Cockpit
projection parity remains outside this BET.
