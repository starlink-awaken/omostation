---
lifecycle: history
owner: governance-team
last_updated: 2026-08-28
title: BET-Y1Q3-T10-48 Resident ledger read-status repair
type: retro
---

# BET-Y1Q3-T10-48 复盘

## Q1 实际耗时 vs appetite

约半天，符合 0.5 day appetite。主要额外耗时来自 scope digest 漂移和
child-first/root-last 交付校验。

## Q2 done_when 是否全部通过

代码、测试和非侵入 host probe 已通过。host ledger 仍被 resident writer
持续占用，因此系统整体不宣称 recovered；该 BET 的目标是 bounded、truthful
read status，而不是消除 writer。

## Q3 与计划不符的事实

`LedgerBroker.connect()` 的 schema/WAL 初始化会与常驻 writer 争用；status
此前没有重试预算，导致瞬时锁直接表现为 degraded。SQLite quick_check 正常，
排除了明显数据库损坏。

## Q4 净增减

- 净增：lock/busy bounded retry、broker close invariant、锁耗尽错误语义和
  9 个 resident status 单测。
- 净减：无数据库、WAL、launchd 或运行态文件修改。

## Q5 给下一个 owner

- 不要把 retry 误报成 lock 已消除；当前 host 仍需要定位 resident writer 的
  长事务/连接生命周期。
- 如需改变 `LedgerBroker.connect()` 的读写模式，必须扩展本 BET Spec 和
  write_surfaces，另行验证，不能在 status slice 中隐式引入第二连接路径。

## Evidence

- OMO regression: `122 passed`.
- Child PR #110: `b170d95ae509499f2b3cfa762c944d7609a7a284`.
- Root PR #2501: `69370b508e8e27758c1663104446a09182e3ae89`.
- Host probe: `database is locked; retry budget exhausted`; ledger bytes,
  mtime and SHA-256 unchanged.

## 正式 closeout（2026-08-29）

本次仅收口 observer 侧交付账务：实现已合入 OMO main，focused tests、root
GaC、SSOT 和 workflow verification 均通过；completion matrix 为
`engineering=VERIFIED`、`operational=PROVEN`、`value=ACCEPTED`、
`overall=outcome_accepted`。host writer-side lock ownership 仍需独立运行态
调查，本 BET 不宣称数据库或 resident writer 已恢复。
