---
schema_version: retro/v1
lifecycle: history
type: bet-retrospective
owner: governance-team
created: 2026-08-29
last_updated: 2026-08-29
bet_id: BET-Y1Q3-T10-88
---

# BET-Y1Q3-T10-88 retrospective

## Q1. What was intended?

Extend the quarantine owner safely for one regular file, then remove only
`卫健委/cleanup-commit.sh` from Documents and close the work-runtime family
without touching neighboring work content.

## Q2. What happened?

The owner gained single-file source support with L4 classification and
lexical/symlink guards. Twelve tests passed. Exactly one regular file moved to
protected Workspace quarantine with a hash-valid rollback manifest.

## Q3. What was discovered?

The prior directory-only owner could not safely target the cleanup wrapper
without also scanning unrelated `_storage` scripts. The single-file mode
closed that gap while preserving the existing directory and dangling-symlink
paths.

## Q4. Surface accounting

Documents lost one regular runtime file and Workspace gained one one-file
quarantine package plus manifest (`3916` bytes). No adjacent content, contract,
cache, schedule, client, public-runtime target, or other migration family
changed. The work-runtime registry moved from `pending` to `retired` only after
the final scoped scan returned no runtime/cache/invalid artifacts.

The required D2 tracked-surface receipt at closeout was: `src_loc=949403`
(`+222991` from the 2026-08 baseline), `test_loc=481392` (`+130538`),
`src_files=4218`, `test_files=2320`, `adr_total=393`, `gac_required=28`,
`bin_scripts=710`, `standards=56`, and `collab_scenarios=5`. Since 2026-08,
the project numstat net was `_root +207459`, `cockpit +31099`, `agora +9165`,
`omo +42280`, `ecos +30281`, `runtime +21838`, `omlxc +25496`,
`l4-kernel +2969`, `aetherforge +213`, and `metaos +29`; `test_loc` did not
decline.

## Q5. What should the next agent know?

`work-runtime` is now terminally retired in the migration registry, but the
Documents plane is not globally pure. Continue with independent recovery and
owner-parity work for `public-runtime`, `root-oneoff-assets`, and
`documents-client-state`; keep their missing rollback evidence fail-closed.

## 批量收口授权补记 (2026-08-30)

principal 授权: "授权T10隔离批量执行" (2026-08-30 会话)。隔离事务与消费证据已由交付 PR 落 main (报告见 docs/reports/); 本补记授权台账批量收口 (矩阵 + complete)。
