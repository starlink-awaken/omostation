---
lifecycle: entry
owner: auto-fix-loop
last_updated: 2026-08-29
---
# BET-Y1Q3-T10-60 retro

## Q1. What changed?

The large capability-sync compatibility CLI was split along a natural boundary
without changing its public command surface. Verification helpers now live in a
library module; the executable retains compatibility wrappers.

## Q2. What is proven?

- The hard single-file threshold is satisfied: 1,048 lines <= 1,500.
- 253 capability/native/trace regression tests and the Python 3.9 probe pass.
- The extracted helper has no registry writer, dispatcher, provider, or new
  authority role.
- PR #2704 is reachable from mainline at
  `4da346b57463b59073ec2f94f049451e78844fb0`.
- `make check-capability-registry` passes on a fully initialized generator
  surface; partial-clone false drift was not committed.

## Q3. What remains?

The workspace still has unrelated god-module errors/warnings outside
`bin/capability-sync.py`. They remain outside this slice. No capability
semantics or Documents physical state is changed by this repair.

## Phase 2 补记 (2026-08-30)

mesh verification 块 (常量 + 9 个函数, ~440 行) 迁入 helper 库; CLI
1,490→1,048 行（以当前主线实际 wc -l 为准）。
测试 patch 面随迁 (monkeypatch 目标改为 helper 模块, 与 CLI 共享同一模块实例)。
定向 253 passed；严格 god-module 全局命令仍因五个无关历史 error 返回非零，
但 scoped predicate 已确认 capability-sync 不在 hard-error 列表。

## Q4. What was the delivery and rollback boundary?

The implementation is preserved by mainline merge commit
`4da346b57463b59073ec2f94f049451e78844fb0`. Rollback is a normal revert of
the helper extraction and its focused tests; no registry or runtime rollback is
required.

The inherited T1-12 engineering test receipt was re-attested to the current
test-file digest because this behavior-preserving split changed that tracked
test file. No T1-12 status or value claim changed.

## Q5. What remains unknown?

This slice proves engineering and projection integrity only. It does not prove
principal-bound value, Documents physical purity, or a new capability semantic.
