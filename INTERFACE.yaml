# Agora Interface Registry
# 文件: projects/agora/INTERFACE.yaml

project: agora
layer: I0
description: MCP 服务发现/路由/代理 Hub

cli:
  - name: agora
    module: agora.cli:main
    description: MCP 服务汇聚中心 CLI (35 子命令)
  - name: agora-mcp
    module: agora.server.mcp:main
    description: 统一 MCP 服务器 (42 tools, stdio)
  - name: agora-mcp-gateway
    module: agora.auth.mcp_gateway:main
    description: 集中式 MCP 后端注册

mcp:
  server: agora.server.mcp
  tools: 42
  transport: [stdio, http, sse]
  proxy_enabled: true

http:
  - port: 7422
    protocol: FastMCP HTTP
    note: "BOS MCP HTTP transport, agent-to-agent (Phase 9 Trust enabled)"
  - port: 7431
    protocol: FastMCP SSE
    note: "Remote Agent gateway, requires --sse flag"

dependencies:
  runtime: [fastmcp, httpx, aiohttp, fastapi, uvicorn, structlog, cryptography, prometheus-client, psutil]
  dev: [pytest, pytest-asyncio, ruff, mypy]

tests: 1112  # as_of 2026-07-28, 见 pytest tests/ (CI 守, 不硬编码)
