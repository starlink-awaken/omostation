#!/usr/bin/env bash
# ==============================================================================
# bin/omo-top — Multi-Agent Swarm 全屏实时协作交互大盘 (Textual 1.x)
#
# 快捷键:
#   [1] Overview 4 象限主屏
#   [2] Agents & Concurrency Locks 深度页
#   [3] Submodules 19 子仓指针雷达
#   [4] Live A2A Communication 事件流
#   [r] 立即强制刷新 | [Space] 暂停/继续 | [q] 退出
# ==============================================================================
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export WORKSPACE_ROOT="$REPO_ROOT"

exec uv run --project "$REPO_ROOT/projects/cockpit" --extra tui python -m cockpit.tui.swarm_app "$@"
