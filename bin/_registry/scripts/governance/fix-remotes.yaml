schema: script-registry/v1
id: bin/ssot/fix-remotes.sh
name: Fix Root and Submodule origin URLs
category: governance
owner: governance-team
description: "自动修复 root + 16 个 submodule 的 origin 指向 omostation canonical. idempotent, 仅在 URL 不匹配时修复."
inputs:
  - name: "无 (脚本读 .gitmodules 自动)"
outputs:
  - type: exit_code
dependencies:
  - bin/gac/remote-hygiene-check.py
triggers:
  - "manual: 当 remote-hygiene check fail 时复制 hint 命令执行"
related: []
maturity: draft
last_reviewed: "2026-09-11"
