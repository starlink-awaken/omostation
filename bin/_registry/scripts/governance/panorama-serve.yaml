schema: script-registry/v1
id: bin/panorama/panorama-serve.py
name: Panorama panorama-serve (织星全景驾驶舱)
category: governance
owner: governance-agent-steward
description: >-
  织星全景驾驶舱 v2 组件。collect=只读 SSOT 聚合（门禁/CI/Cron/子模块/债务/BET/Agent/ASD）；
  serve=静态服务 :43910。零写入零派工。
inputs:
  - name: --json / --gates / --check-side-effects / --port
    type: flag
    required: false
outputs:
  - type: exit_code
triggers:
  - manual
  - launchd
tags:
  - panorama
  - dashboard
  - observability
