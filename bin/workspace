#!/bin/bash
# workspace — 统一用户入口
# 添加到 PATH 或运行: workspace <command>
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
export PYTHONPATH="$PROJECT_ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec /opt/homebrew/bin/python3.14 -m wksp "$@"
